/**
 * Access descriptor registration barrel.
 *
 * Deck's current top-level client root (`src/app/page.tsx`) should import this
 * barrel as a side-effect so descriptor registrations run before any consumer mounts.
 */
import "./wecom-access-descriptor";
