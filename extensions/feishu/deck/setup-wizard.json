{
  "steps": [
    {
      "id": "mode",
      "type": "radio",
      "title": "$t:plugin.feishu.step1Title",
      "options": [
        {
          "value": "websocket",
          "label": "$t:plugin.feishu.modeWebSocket",
          "description": "$t:plugin.feishu.modeWebSocketDesc",
          "badge": "$t:wizard.recommended"
        },
        {
          "value": "webhook",
          "label": "$t:plugin.feishu.modeWebhook",
          "description": "$t:plugin.feishu.modeWebhookDesc"
        }
      ]
    },
    {
      "id": "creds",
      "type": "form",
      "title": "$t:plugin.feishu.step2Title",
      "schema": {
        "type": "object",
        "required": ["appId", "appSecret"],
        "properties": {
          "appId": {
            "type": "string",
            "title": "$t:plugin.feishu.appId",
            "description": "$t:plugin.feishu.appIdHelp",
            "placeholder": "$t:plugin.feishu.appIdHint"
          },
          "appSecret": {
            "type": "string",
            "format": "password",
            "title": "$t:plugin.feishu.appSecret",
            "description": "$t:plugin.feishu.appSecretHelp",
            "placeholder": "$t:plugin.feishu.appSecretHint"
          }
        }
      }
    },
    {
      "id": "probe",
      "type": "action",
      "title": "$t:plugin.feishu.step3Title",
      "description": "$t:plugin.feishu.testDesc",
      "action": "channel.feishu.probe",
      "successMessage": "$t:plugin.feishu.probeSuccess",
      "failureMessage": "$t:plugin.feishu.probeFailed"
    }
  ],
  "onComplete": {
    "action": "channel.feishu.saveConfig",
    "params": {
      "connectionMode": { "$ref": "$steps.mode.value" },
      "appId": { "$ref": "$steps.creds.value.appId" },
      "appSecret": { "$ref": "$steps.creds.value.appSecret" }
    }
  }
}
