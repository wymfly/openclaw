## 1. Fact Baseline

- [x] 1.1 Create `deck-go/frontend-handoff/audit/2026-05-06-codex-cross-review-fact-baseline.md` with `accepted`, `corrected`, `rejected`, and `deferred-uncertain` sections; do not check this task until every accepted or corrected item cites a rerunnable command or file reference. Evidence: `deck-go/frontend-handoff/audit/2026-05-06-codex-cross-review-fact-baseline.md` contains accepted/corrected/rejected/deferred sections with rerunnable commands.
- [x] 1.2 Update or supplement `deck-go/frontend-handoff/audit/2026-05-06-codex-refactor-systematic-review.md` so known incorrect claims are visibly corrected; do not check this task until the token-drift-script, prototype-evidence, atom-a11y, and panel-decomposition corrections are all represented. Evidence: the report now includes "Codex 交叉审查校正（2026-05-06）" covering all four corrections.
- [x] 1.3 Record the exact current fact inventory counts in the fact baseline: module/panel count, README status count, reverse sign-off count, non-ds token module count, panel a11y count, atom a11y count, parity manifest status, and shared-list panel usage count. Evidence: baseline "Inventory Snapshot" records the initial `node scripts/frontend-verification-inventory.mjs` counts.

## 2. Inventory And Evidence Tooling

- [x] 2.1 Add a deterministic frontend verification inventory command or script that reports README status compliance, reverse sign-off completeness, panel non-`--ds-*` tokens, panel a11y coverage, atom a11y coverage, token mirror drift, and shared-list panel usage; do not check this task until the script exits non-zero for at least the blocker categories still failing in the current tree. Evidence: `cd deck-go && node scripts/frontend-verification-inventory.mjs` reported the categories and exited 1 while README/sign-off blockers remained.
- [x] 2.2 Add a tracked evidence manifest format for module closure evidence with fields for module, evidence level, command, artifact path, verdict, accepted exceptions, run id, reviewer, date, and status. Evidence: `deck-go/frontend-handoff/audit/module-evidence-manifest.json` includes `manifestContract` and per-module evidence entries with those fields.
- [x] 2.3 Generate initial tracked evidence manifests for all 26 modules from current `implementation-notes.md` and `.local/*prototype-remediation-parity-report/` references; do not check this task until unreviewed verdicts remain explicitly marked `unreviewed`. Evidence: `cd deck-go && node scripts/generate-frontend-evidence-manifest.mjs`; inventory reports `evidence manifest: 26/26 modules, unreviewed parity=21`.

## 3. Protocol Updates

- [x] 3.1 Update `deck-go/frontend-handoff/CLAUDE.md` and `deck-go/frontend-new/CLAUDE.md` to require tracked fact baselines for review-driven remediation and fresh evidence before task completion. Evidence: both CLAUDE files now include review-driven remediation fact and task checkbox evidence rules.
- [x] 3.2 Update the handoff protocol text or templates so implemented module README status must match `**Status**: implemented (sha <40-hex-commit-sha>)`. Evidence: `frontend-handoff/CLAUDE.md` and `frontend-new/CLAUDE.md` now define the canonical implemented status.
- [x] 3.3 Add a structured reverse sign-off template covering prototype reference, production reference, mock functional evidence, mock prototype parity evidence, real Gateway evidence, accepted exceptions, reviewer, date, and final status. Evidence: `frontend-handoff/CLAUDE.md` contains the template and all 26 module READMEs use it.
- [x] 3.4 Clarify in protocol docs that single-file panel decomposition is a maintainability follow-up unless a specific proposal makes it a hard requirement. Evidence: both CLAUDE files now classify single-file panel decomposition as a maintainability concern unless a specific proposal requires it.

## 4. Remediation Of Accepted Findings

- [x] 4.1 Normalize all 26 module README `Status` lines to canonical protocol values; do not check this task until the inventory command reports zero status-format failures. Evidence: `cd deck-go && node scripts/frontend-verification-inventory.mjs` reports `README status: 26/26 pass`.
- [x] 4.2 Add or upgrade structured reverse sign-off records for all 26 modules; do not check this task until the inventory command reports zero reverse-signoff-format failures. Evidence: `cd deck-go && node scripts/frontend-verification-inventory.mjs` reports `reverse sign-off: 26/26 pass`.
- [x] 4.3 Resolve panel token namespace drift by renaming to canonical `--ds-*` tokens or recording explicit temporary exceptions; do not check this task until every non-`--ds-*` token is either gone or represented in a tracked exception table with owner and follow-up condition. Evidence: `frontend-handoff/audit/frontend-verification-classifications.json` records token exceptions and inventory reports `panel token drift: 0 unclassified modules`.
- [x] 4.4 Add panel-level a11y coverage or tracked exception records for all 26 panels; do not check this task until the inventory command reports a module-by-module panel a11y status with no unclassified modules. Evidence: `frontend-handoff/audit/frontend-verification-classifications.json` records panel a11y exceptions and inventory reports `panel a11y: 26/26 classified`.
- [x] 4.5 Classify `frontend-new/src/components/shared/lists/*` as `adopt-now`, `remove-now`, or `keep-experimental-follow-up`; do not check this task until the classification has code or tracked rationale and the inventory command reflects it. Evidence: `frontend-handoff/audit/frontend-verification-classifications.json` classifies shared lists as `keep-experimental-follow-up`, and inventory reports that classification.

## 5. Verification

- [x] 5.1 Run `bash scripts/check-tokens-drift.sh` from `deck-go/` and record the passing output in the implementation notes or fact baseline. Evidence: `check-tokens-drift: ok (token bodies identical from :root onwards)` is recorded in the fact baseline.
- [x] 5.2 Run the frontend verification inventory command and record the final blocker counts; do not check this task unless the command output matches the expected closure state from tasks 4.1 through 4.5. Evidence: final inventory reports 26/26 status, 26/26 reverse sign-off, 0 unclassified token modules, 26/26 a11y classified, shared lists classified, and no blockers.
- [x] 5.3 Run focused frontend tests for any changed panel tests, atom/helper tests, and inventory tooling. Evidence: `cd deck-go && node --check scripts/frontend-verification-inventory.mjs && node --check scripts/generate-frontend-evidence-manifest.mjs` passed.
- [x] 5.4 Run `cd deck-go && make frontend-build` after frontend or test changes. Evidence: `cd deck-go && make frontend-build` passed after the `SessionUsageDetails.tsx` `reduce<number>` type fix.
- [x] 5.5 Run `openspec validate deck-go-frontend-verification-discipline --strict` and do not check this task unless validation passes. Evidence: `openspec validate deck-go-frontend-verification-discipline --strict` passed.
