changeName: deck-go-models-config-control-plane
archiveReady: true
gapCount: 0
generatedAt: "2026-05-09T23:30:00+08:00"
notes:
  - "Codex cross-review repaired code-truth drift left by the prior implementation: OpenClaw provider models persist as ModelDefinitionConfig[] arrays, SecretInput writes use real SecretRef {source, provider, id} objects, input modalities are text/image only, optional boolean toggles use enable/disable/preserve actions, model cost no longer invents unit, and impact preview responses always return non-null references arrays."
  - "Mock browser E2E is now retargeted to the typed config-authority UI and passes. It also caught a guardrail bug: ImpactPreviewDialog previously allowed destructive/mode commits without the typed confirmation step. ModelsPanel now enforces preview -> Continue -> TypeToConfirmDialog -> commit."
  - "Bounded real Gateway E2E was attempted with the isolated real stack, but blocked before Models code by shared real-stack bootstrap: waitForBundledRuntime did not observe a bundled runtime pid before Playwright's 300000ms timeout. Per proposal circuit-breaker rules this is recorded as a real-environment handoff; code-level contract/backend/frontend/mock E2E evidence remains mandatory and green."
scenarios:
  - scenarioId: models-control.truth-source-map
    ownerTask: "1.1-1.5"
    status: pass
    verificationCommand: "Inspect OpenClaw config types/zod/help, Gateway config write routes, deck-go current contracts, and Models frontend/backend paths"
    evidence:
      - "src/config/types.models.ts ModelsConfig + ModelConfig + ModelProviderConfig field optionality enumerated and reflected in DeckGoModelsConfigDetail"
      - "src/config/zod-schema.core.ts modelsConfigSchema parsed for required vs optional + enum values; reflected in typed BFF validation"
      - "src/config/schema.help.ts descriptive help mapped to drawer copy"
      - "Gateway config.get / config.patch behavior (no native idempotency / audit / rollback) explicitly documented in design.md and tasks.md"
      - "deck-go contract chain audit recorded in proposal.md driftClassification (repair-now / defer / reject)"
  - scenarioId: models-control.contract-chain
    ownerTask: "2.1-2.6"
    status: pass
    verificationCommand: "cd deck-go && make contract-gate"
    evidence:
      - "contract-gate green: stream/live-projection/list-query/mutation-evidence/dynamic-surfaces/route-governance/config-write-safety all up to date"
      - "gateway-typecheck 0/0 violations"
      - "gateway-describe-completeness ok 173 methods/24 events"
      - "contract-chain-audit ok 26 rows"
      - "DeckGoModelsConfigDetail / DeckGoModelProviderUpsertRequest / DeckGoModelImpactPreview / SecretInputStatus / DeckGoModelSecretRef / DeckGoModelBooleanToggleAction are present in contracts/source/deck-api.contract.ts and round-trip through contracts/generated/ts/* + backend/internal/deckapi/types.generated.go"
  - scenarioId: models-control.generated-artifacts
    ownerTask: "2.5,7.2"
    status: pass
    verificationCommand: "cd deck-go && make contract-gate"
    evidence:
      - "make contract-gate green (covers contract sync + generated artifact freshness)"
      - "no upstream Gateway protocol changes required, so make protocol-check intentionally not run"
  - scenarioId: models-control.bff-config-patch
    ownerTask: "3.1-3.5"
    status: pass
    verificationCommand: "cd deck-go/backend && go test ./internal/server/... -run 'TestModels' -count=1"
    evidence:
      - "ok github.com/openclaw/openclaw/deck-go/backend/internal/server 0.422s"
      - "Backend covers /models/config/detail projection, provider/model upsert, source-truth provider model arrays, partial-config preservation, expectedBaseHash threading, real SecretRef object writes, text/image modality enforcement, and explicit boolean disable actions"
  - scenarioId: models-control.reference-impact
    ownerTask: "1.4,3.6-3.8"
    status: pass
    verificationCommand: "cd deck-go/backend && go test ./internal/server/... -run 'TestModels' -count=1"
    evidence:
      - "ok github.com/openclaw/openclaw/deck-go/backend/internal/server 0.422s covers modelReferenceIndex, provider/model delete preview + commit with impactToken + service-side rescan + stale-preview rejection, and mode dry-run vs commit with catalog/config-derived impact preview"
  - scenarioId: models-control.secret-safety
    ownerTask: "3.2,3.4,4.3,5.6,5.8"
    status: pass
    verificationCommand: "cd deck-go/frontend-new && pnpm vitest run src/data/modules/models/mutations.test.ts src/components/panels/models/ModelsPanel.test.tsx"
    evidence:
      - "mutations.test.ts 11/11 verifies typed mutation routing and advanced raw fallback isolation"
      - "ModelsPanel.test.tsx wizard typed mutation routing test asserts apiKey is constructed as { action: 'set-ref', ref: { source: 'env', provider: 'default', id: 'ANTHROPIC_API_KEY' } }"
      - "i18n copy in src/i18n/{en,zh}.json avoids unsupported audit / rollback / quota / rate-limit / secret-store / OAuth claims"
  - scenarioId: models-control.frontend-data
    ownerTask: "4.1-4.5"
    status: pass
    verificationCommand: "cd deck-go/frontend-new && pnpm vitest run src/data/modules/models/"
    evidence:
      - "mutations.test.ts 11/11 covering typed mutation routing, dryRun-vs-commit invalidation split (mode dryRun does NOT invalidate; mode commit invalidates configDetail/config/configured/catalogProviders), advanced raw escape hatch isolation (saveModelsConfig invalidates modelsKeys.all() and does not call typed mutation helpers), conflict propagation through DataFabricError kind: 'conflict'"
      - "src/api.ts wrappers fetchModelsConfigDetail / upsertModelProvider / previewProviderDelete / deleteModelProvider / upsertModel / previewModelDelete / deleteModel / setModelsCatalogMode consume @/api-types generated DTOs only"
      - "saveModelsConfig audit confirms only the advanced raw editor surface invokes useSaveModelsConfigMutation; product-flow mutations route through typed hooks"
  - scenarioId: models-control.frontend-product-ui
    ownerTask: "5.1-5.9"
    status: pass
    verificationCommand: "cd deck-go/frontend-new && pnpm vitest run src/components/panels/models/ && cd deck-go && make frontend-build"
    evidence:
      - "ModelsPanel.test.tsx 6/6: catalog header + provider list rendering, empty-state CTA, AddProvider wizard typed mutation routing with SecretRef construction, advanced raw editor opens without firing typed mutations, delete preview surfaces references then requires Continue before confirmText: 'delete' + impactToken: 'tok-1', Chinese locale 新增提供方 / 高级原始编辑"
      - "Component tree under src/components/panels/models/ split: lib/models-selectors.ts + parts/{CatalogHeader,ProviderListSection,ModelRow}.tsx + drawers/{ProviderDrawer,ModelDrawer,SecretInputField}.tsx + wizard/AddProviderWizard.tsx + dialogs/{ImpactPreviewDialog,TypeToConfirmDialog}.tsx + ModelsPanel.tsx orchestrator"
      - "make frontend-build ✓ built in 1.51s"
      - "i18n keys for English/Chinese product copy fully migrated"
  - scenarioId: models-control.handoff-sync
    ownerTask: "6.1-6.4"
    status: pass
    verificationCommand: "Review frontend-handoff/modules/models and openspec/follow-ups changes"
    evidence:
      - "frontend-handoff/modules/models/{README.md, components.md, interactions.md, states.md, api-usage.md, implementation-notes.md} rewritten to reflect typed contract chain"
      - "frontend-handoff/modules/models/prototype.html rewritten as self-contained typed config-authority workbench mockup; no usage/cost/audit/probe surfaces"
      - "Historical V1 prototype-v1-codex.html and V2 multi-file bundle preserved as labeled references; V2 HTML shell preserved verbatim as prototype-v2-list-detail.html"
      - "openspec/follow-ups/2026-05-09-deck-go-models-config-control-plane-follow-ups.md created with 10 entries covering model default editing, rate-limit/schema additions, OAuth runner, secret-store CRUD, probe runner, search/filter, dynamic provider quirks, catalog authType permissive handling, V2 leftovers, and the real-stack bundled runtime bootstrap timeout"
  - scenarioId: models-control.mock-e2e
    ownerTask: "7.5"
    status: pass
    verificationCommand: "cd deck-go && make e2e-mock-module MODULE=models"
    evidence:
      - "test/e2e/models-visual.spec.ts retargeted from historical V2 list/detail UI to the typed config-authority workbench."
      - "1/1 passing in 4.4s. Covers catalog header, provider grouped list, ProviderDrawer, ModelDrawer, SecretInputField set-ref state, AddProviderWizard typed save and readback, advanced raw editor entry, provider delete preview -> Continue -> TypeToConfirmDialog -> commit, and replace mode preview -> typed confirm -> commit."
      - "test/fixtures/mock-gateway.mjs config.patch now mutates the in-memory configFixture so typed mock saves can be read back through config.get/config.detail."
  - scenarioId: models-control.real-smoke
    ownerTask: "7.6"
    status: handoff-blocked
    verificationCommand: "cd deck-go && make e2e-real-module MODULE=models"
    evidence:
      - "test/e2e/models-real-gateway.spec.ts retargeted to typed BFF actions only: config detail, provider upsert, model upsert, mode dry-run, model delete preview/commit, provider delete preview/commit, and run-scoped cleanup."
      - "Bounded run blocked before the Models test body by shared real-stack bootstrap. Error: Test timeout of 300000ms exceeded in helpers.ts waitForBundledRuntime; /api/runtime/gateway did not expose a bundled pid in time."
      - "Stack root recorded by Playwright: /var/folders/43/vr1f5k0j64764m76drn3wxlw0000gn/T/deck-go-real-gateway-e2e-IqWHc3. Isolation evidence shows copied config from /Users/wangym/.openclaw/openclaw.json and sanitized external channel/plugin entries."
      - "Per proposal circuit breaker, this remains a real-environment handoff rather than a code-completion blocker because all code-level and mock-browser gates are green."
  - scenarioId: models-control.final-validation
    ownerTask: "7.1-7.8"
    status: pass
    verificationCommand: "openspec validate deck-go-models-config-control-plane --type change --strict plus relevant deck-go checks"
    evidence:
      - "7.1 openspec validate --type change --strict: rerun before archive."
      - "7.2 cd deck-go && make contract-gate: green after regenerating required governance outputs."
      - "7.3 cd deck-go/backend && GOCACHE=/tmp/deck-go-gocache GOSUMDB=off go test ./internal/server/... -run 'TestModels|TestModelReference' -count=1: ok 0.422s; race smoke for modelsControlImpactStore concurrent access: ok 1.289s."
      - "7.4 cd deck-go/frontend-new && pnpm vitest run src/data/modules/models/ src/components/panels/models/: 17/17 passing; cd deck-go && make frontend-build: built successfully in 1.51s."
      - "7.5 cd deck-go && make e2e-mock-module MODULE=models: 1/1 passing in 4.4s."
      - "7.6 cd deck-go && make e2e-real-module MODULE=models: handoff-blocked by real-stack waitForBundledRuntime timeout before Models code path; exact blocker recorded above."
      - "7.7 this verification.yaml is updated with archiveReady=true and gapCount=0 under the proposal's real-E2E circuit breaker; the real-stack bootstrap handoff is tracked in openspec/follow-ups/2026-05-09-deck-go-models-config-control-plane-follow-ups.md as FU-010."
      - "7.8 openspec archive deck-go-models-config-control-plane --yes synced accepted spec deltas into openspec/specs/** and archived the change as 2026-05-09-deck-go-models-config-control-plane."
