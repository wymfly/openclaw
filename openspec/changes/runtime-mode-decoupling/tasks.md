## 1. Backend foundation — packages, env, state, mode-switched main

- [x] 1.1 Create `internal/runtime/facade/` package with `RuntimeFacade` interface, `Capabilities` struct, `RemoteEndpointInput` / `TestResult` / `StreamEvent` types, and `ErrUnsupported` / `ErrNotConfigured` sentinel errors
- [x] 1.2 Create `internal/runtime/envconf/` package: `RuntimeMode` type, `RuntimeBundledConfig`, `RuntimeRemoteDefaults`, `Loaded` struct, `Load(opts)` function that parses process env (and optional `.env` file via `DECK_DOTENV_FILE`)
- [x] 1.3 Add `envconf_test.go` covering: missing `RUNTIME_MODE` → error; invalid value → error; bundled with missing `RUNTIME_BUNDLED_COMMAND` → error; pass-through env prefix parsing (`RUNTIME_BUNDLED_ENV_*`); remote with all-empty endpoint → first-run defaults; quoted values
- [x] 1.4 Create `internal/runtime/state/` package: `DeckState` schema (`Appearance`, `Notifications`, `PairedDevices`, optional `Remote`), `Open(path)`, `Read()`, `WriteRemote(...)` with atomic temp-file + rename, file-not-found auto-create, corrupt-file warning behavior
- [x] 1.5 Add `store_test.go` covering: first-write file creation; atomic rename; corrupt file does not auto-overwrite; concurrent reads; write-failure surfaces error
- [x] 1.6 Create `internal/runtime/shared/` package with mode-agnostic Gateway RPC client base (HTTP/WS, token header injection, timeouts, retry policy)
- [x] 1.7 Create `internal/runtime/bundled/` package skeleton; relocate the following files from `internal/runtime/` into `internal/runtime/bundled/`: `supervisor.go`, `supervisor_test.go`, `preflight.go`, `preflight_test.go`, `ownership.go`, `fingerprint.go`, `process_group_unix.go`, `process_group_windows.go`, and any other supervisor-adjacent files. (Note: `config_sync.go` / `config_sync_test.go` are NOT in this relocation set — they are deleted in 1.9.)
- [x] 1.8 Update import paths repo-wide for the relocation; verify `go build ./...` passes
- [x] 1.9 Delete `internal/runtime/config_sync.go` and `config_sync_test.go` (after relocating tests for any logic worth preserving)
- [x] 1.10 Implement `bundled.New(cfg *RuntimeBundledConfig) (RuntimeFacade, error)` that wraps the relocated supervisor, satisfying the `RuntimeFacade` interface (mode-incompatible methods return `ErrUnsupported`)
- [x] 1.11 Create `internal/runtime/remote/` package skeleton: `New(defaults, store)`, stubbed `Health/Status/Summary/Call/Stream/UpdateRemoteEndpoint/TestRemoteEndpoint`. Lifecycle methods return `ErrUnsupported`. Real RPC client wiring deferred to Phase 3 task 3.x.
- [x] 1.12 Add `facade.BuildFacade(cfg *envconf.Loaded, store *state.Store) (RuntimeFacade, error)` helper that switches on `cfg.Mode` and instantiates `bundled.New` or `remote.New`; both `cmd/deck-go/main.go` and `cmd/controld/main.go` SHALL call this single helper rather than each containing their own switch
- [x] 1.13 Refactor `cmd/deck-go/main.go` to call `envconf.Load(...)`, open state store, call `facade.BuildFacade(...)`, inject the resulting facade into HTTP routes, and call `controld.RunServer(ctx, server, rt, "deck-go")`
- [x] 1.14 Refactor `cmd/controld/main.go` similarly so its assembly mirrors `deck-go/main.go` via the same `facade.BuildFacade(...)` call
- [x] 1.15 Run `go test ./internal/runtime/bundled/...` and verify all relocated supervisor tests pass; ensure no `internal/runtime/config_sync*` references remain
- [x] 1.16 Add a CI/lint step (or extend existing import-cycle check) that fails the build if `internal/runtime/bundled/` imports `internal/runtime/remote/` (or vice versa), and if `internal/runtime/shared/` imports `bundled/` / `remote/` / `facade/` (shared MUST be a leaf)
- [x] 1.17 Add `envconf.Load` permission check on any file referenced by `DECK_DOTENV_FILE`: POSIX requires mode `<= 0600`; Windows requires owner-only DACL (no read access for principals beyond current user / `SYSTEM` / `Administrators`); refuse-to-boot on either failure with exit 64; process-env-wins-over-file conflict logged at INFO level
- [x] 1.18 Add denylist enforcement for `RUNTIME_BUNDLED_ENV_*` covering `LD_PRELOAD`, `LD_LIBRARY_PATH`, `LD_AUDIT`, `LD_BIND_NOW`, all `DYLD_*`, and `PATH`; honor an additive operator denylist via `RUNTIME_BUNDLED_ENV_DENY=A,B,C`; refuse-to-boot on hit (exit 64) with stderr naming the offending key

## 2. API surface — capabilities, endpoint family, deletions, settings narrowing

- [x] 2.1 Implement `GET /api/runtime/capabilities` handler returning `{mode, configured, endpointMutable, supervisorState}`; wire into existing route registration
- [x] 2.2 Implement `GET /api/runtime/endpoint` handler returning the active endpoint with token redacted (`tokenConfigured: bool`) and `source` field (`"env"` or `"json"`)
- [x] 2.3 Implement `PUT /api/runtime/endpoint` handler: bundled mode → `405 endpoint_not_mutable`; remote mode → validate body has full `{url, token, tlsVerify}` where `token` is either plaintext or the explicit `__unchanged__` sentinel, call `facade.UpdateRemoteEndpoint`, return new GET response shape
- [x] 2.4 Implement validation in PUT handler for `invalid_url`, `token_required`, `invalid_tls_verify` returning HTTP 400 with the error code in body
- [x] 2.5 Implement `POST /api/runtime/endpoint:test` handler: empty body uses active config; non-empty body uses candidate; calls `facade.TestRemoteEndpoint`
- [x] 2.6 Delete HTTP route registrations in `deck-go/backend/internal/server/runtime.go` for `POST /api/runtime/gateway/start`, `POST /api/runtime/gateway/restart`, `POST /api/runtime/gateway/stop` (the route mux entries and their handlers; verify with `grep -n "/runtime/gateway/" deck-go/backend/internal/server/runtime.go` returns only the GET-only path remaining); preserve the internal stop function only as a hook called from `controld.RunServer` graceful shutdown path
- [x] 2.7 Narrow `GET /api/settings` response: drop `managedGateway` field; convert `accessToken` to `accessTokenConfigured: boolean`; keep `appearance/notifications/pairedDevices`
- [x] 2.8 Narrow `PUT /api/settings` request validation: accept only `appearance/notifications/pairedDevices`; reject `managedGateway`/`accessToken` (and any other dropped field) with HTTP 400 listing the unaccepted field name
- [x] 2.9 Strip token carry-forward logic from `runtime/openclaw/legacy_admin_settings_onboarding.go`; update tests to match
- [x] 2.10 Update `runtime/openclaw/legacy_runtime_orchestration.go` import paths and any references to relocated supervisor types
- [x] 2.11 Update `deck-go/contracts/source/deck-api.contract.ts`: add types for `Capabilities`, `EndpointGetResponse`, `EndpointPutRequest`, `EndpointTestRequest/Response`; remove `start/restart/stop` action types if generated; narrow Settings types
- [x] 2.12 Run `cd deck-go && make contracts-sync` to regenerate `deck-api.generated.ts` + `deckapi.generated.go` after editing `deck-go/contracts/source/deck-api.contract.ts`; then `make contracts-check` to verify in-sync (the pre-merge gate)
- [x] 2.13 Implement / extend the unified error-code taxonomy: `endpoint_not_mutable`, `gateway_not_configured`, `invalid_url`, `token_required`, `invalid_tls_verify`, `gateway_unreachable`, `gateway_auth_failed`, `tls_verification_failed`; ensure consistent body shape `{code, message}`
- [x] 2.14 Update `internal/server/server_test.go` with both-mode fixtures (one suite under `RUNTIME_MODE=bundled`, one under `RUNTIME_MODE=remote`)
- [x] 2.15 Update `internal/controld/runserver_test.go` so the mock satisfies the minimal `RuntimeFacade` subset rather than the prior `stubRuntimeStopper` interface
- [x] 2.16 Reshape the existing `GET /api/runtime/gateway` handler in `deck-go/backend/internal/server/runtime.go:18-19` so the returned payload field set is selected by `capabilities.supervisorState`: bundled mode → `{mode: "bundled", pid, ownershipState, restartAttempts}`; remote mode (configured) → `{mode: "remote", lastConnectedAt, lastError, latencyP50, tlsVerified}`; remote mode (first-run, `configured: false`) → 503 `gateway_not_configured` (handled by the 503 middleware from 3.6, not bypassed). Update `RuntimeGatewayStatusResponse` in `deck-go/contracts/source/deck-api.contract.ts` to be a discriminated union on `mode`, regenerate via 2.12.

## 3. Remote mode — RPC client, reconnect, first-run, passthrough 503

- [x] 3.1 Implement `internal/runtime/remote/client.go`: HTTP/WS RPC client with TLS verify toggle, custom headers placeholder, token header injection, timeout config
- [x] 3.2 Implement `internal/runtime/remote/reconnect.go`: connection state machine, exponential backoff, `lastConnectedAt`/`lastError` tracking on `RemoteState`
- [x] 3.3 Implement `RemoteFacade.UpdateRemoteEndpoint` entrypoint and basic input validation (URL scheme allowlist, sentinel resolution, body completeness); the full two-phase commit + drain timing semantics live in 3.12/3.13 — this task scopes only the dispatcher and validation layer
- [x] 3.4 Implement `RemoteFacade.TestRemoteEndpoint`: open transient client with provided (or active) config, perform a lightweight `gateway.describe` call, return `{ok, latencyMs, gatewayVersion, tlsVerified, error}` without mutating state
- [x] 3.5 Implement first-run state detection in `RemoteFacade.Capabilities()` based on `state.RemoteIsConfigured()` and env defaults
- [x] 3.6 Wire `gateway_not_configured` 503 response into all Gateway RPC passthrough route handlers under deck-go's `/api/...` mux — concretely: `/api/gateway/health`, `/api/gateway/status`, `/api/gateway/describe`, `/api/activity`, `/api/monitor/runs`, `/api/monitor/runs/{runId}`, `/api/agents`, `/api/agents/{agentId}/...`, plus controld's `/api/v1/runtimes/{runtimeId}/...` admin family — implemented as a single middleware that consults `facade.Capabilities().Configured` and is mounted on both the `/api/...` and `/api/v1/...` chi routers (so newly added passthrough routes inherit the 503 contract without per-route opt-in)
- [x] 3.7 Add `internal/runtime/remote/facade_test.go` covering: first-run capabilities; `UpdateRemoteEndpoint` persistence + reconnect; `TestRemoteEndpoint` does not mutate state; in-flight RPC during endpoint switch is drained, not aborted
- [x] 3.8 Add a mock Gateway server fixture for E2E (`deck-go/test/fixtures/mock-gateway.ts` or equivalent) that responds to `gateway.describe` and a couple of passthrough methods
- [x] 3.9 Run `go test ./internal/runtime/remote/...` and verify all new tests pass
- [x] 3.10 Verify end-to-end: `RUNTIME_MODE=remote` boots in first-run state → operator submits endpoint via API → reconnects → a chat round-trip RPC succeeds against the mock Gateway
- [x] 3.11 Implement drain helper at `internal/runtime/shared/drain.go` with **primitives-only** signature: takes `context.Context`, old/new `io.Closer`, drain timeout `time.Duration`, terminal-event-name string, and a `func(streamID string)` callback for stream termination — does NOT import `internal/runtime/facade/`; terminal-event-name constants (`endpoint_switched`, `reconnect_requested`) are defined in `facade/` and passed in by the caller
- [x] 3.12 Wire `RemoteFacade.UpdateRemoteEndpoint` as a **two-phase** operation: Phase A opens a transient candidate connection and runs `gateway.describe` while the active client continues to serve all callers; on Phase A failure, return the matching error code and leave persistence + active client untouched; on Phase A success, Phase B atomically persists `deck-state.json` and atomically swaps the active client, then invokes the drain helper with `endpoint_switched` for old in-flight callers
- [x] 3.13 Add tests for two-phase + drain semantics: Phase A failure leaves state and active client untouched (no JSON write, no caller routed to candidate); Phase A success → Phase B swap atomically routes new callers to new client; in-flight unary completes against old; in-flight unary times out → 503 `endpoint_switching`; active SSE consumer receives `endpoint_switched` within 1s; admin reload-runtime in remote mode emits `reconnect_requested` (NOT `endpoint_switched`); **crash-recovery subcase**: pre-populate `deck-state.json` with a remote endpoint that the test's mock-Gateway will reject `gateway.describe` for, then boot deck-go and assert that all Gateway RPC passthrough routes return `503 gateway_not_configured` with the `lastError` field populated, until a fresh `PUT /api/runtime/endpoint` (or `.env` edit + restart) repairs the configuration
- [x] 3.14 Implement PUT-with-token-sentinel handling: `token == "__unchanged__"` (byte-exact, case-sensitive, no whitespace trimming) resolves to the currently active token before writing the complete JSON `{url, token, tlsVerify}` triple; this is the only permitted pre-write carry-forward. Any other value is treated as a real token; empty-string `token` → 400 `token_required` (does not clear). Tests SHALL cover **five distinct subcases**, each as a separate test function:
  - 3.14.a positive sentinel: `token: "__unchanged__"` → active token resolved and preserved in the complete JSON write, response `tokenConfigured: true`
  - 3.14.b case mismatch: `token: "__UNCHANGED__"` → treated as real token "**UNCHANGED**" (replace path)
  - 3.14.c whitespace padding: `token: " __unchanged__ "` → treated as real token with surrounding spaces (replace path)
  - 3.14.d length variant: `token: "__unchanged_"` (12 bytes) → treated as real token (replace path)
  - 3.14.e empty rejection: `token: ""` → HTTP 400 with `code: "token_required"`; on-disk token unchanged
- [x] 3.15 Implement URL scheme validation rejecting non-`http`/`https` schemes with `code: "invalid_url"` at **two layers**: (a) `envconf.Load` time for `RUNTIME_REMOTE_URL` (refuse-to-boot with exit 64), and (b) PUT body validation (return HTTP 400 before any network call); add unit tests covering `file://`, `unix://`, `ftp://`, `gopher://`, malformed URLs against both layers

## 4. Frontend — capability-gated rewrite of Settings + Gateway panels

- [x] 4.1 Implement `useCapabilities()` hook in `deck-go/frontend/src/hooks/` using React built-ins only — `useState` + `useEffect` + `fetch` against `GET /api/runtime/capabilities` (via the existing `deckFetch` wrapper in `lib/deck-client.ts`); cache the result in module-scope or via `React.useSyncExternalStore` for cross-component sharing; refresh on `document.visibilitychange === "visible"`. **Do NOT add `@tanstack/react-query` or any other state-management dependency** — `deck-go/frontend/package.json` currently has no client-state library and adding one is out of scope per proposal `Dependencies`
- [x] 4.2 Implement `<ModeBadge capabilities />` component (top-right): green for `bundled`/`remote configured`, yellow for `remote first-run`, hover tooltip with full URL
- [x] 4.3 Implement `<FirstRunBanner />` component: renders only when `capabilities.mode === "remote" && !configured`; full-width, links to Settings, dismissible only after configuration
- [x] 4.4 Implement `<EndpointSection capabilities value onSave onTest />` component: renders URL/token/tlsVerify fields; readonly + lock-icon + `set via .env` badge when `!endpointMutable`; editable + Save button when `endpointMutable`; integrated Test connection button; error mapping for the 4xx error codes. **Save semantics**: always include `token` in the PUT body — when the user has not edited the token field (it shows the redacted placeholder), submit the sentinel `__unchanged__`; backend resolves that sentinel to the active token before writing a complete JSON endpoint object. When the user has typed a new value, submit it as the new token. Never omit the field (omission triggers `400 token_required`).
- [x] 4.5 Implement `<ReadOnlyField label value badge />` generic component for capability-locked fields
- [x] 4.6 Refactor `SettingsPanel.tsx`: remove `managedGateway.command/args/workingDir/env/autoStart/bindHost/bindPort` form fields and their state (`managedGatewayArgsText`, `managedGatewayArgsError`, `updateManagedGateway`); replace `accessToken` `<input>` with `<ReadOnlyField>` showing `(set via .env)` for both modes; mount `<EndpointSection>` at the top of the panel; preserve appearance/notifications/pairedDevices sections unchanged
- [x] 4.7 Refactor `GatewayPanel.tsx` runtime tab: delete Start/Stop/Restart buttons and `runAction` dispatcher; delete `runtime.rawSettings` / `rawDiagnostics` raw-JSON cards; render structured summary card whose field set branches on `capabilities.supervisorState` (PID/Ownership/RestartAttempts vs LastConnected/Latency/TLS-verified); keep overview/timeline/history tabs untouched
- [x] 4.8 Convert `useDeckUI.bootstrap.runtime` shape to a discriminated union: `{mode: "bundled", pid, ownershipState, restartAttempts, ...}` vs `{mode: "remote", lastConnectedAt, lastError, latencyP50, ...}`; update all consumers to type-narrow
- [x] 4.9 Update `deck-go/frontend/src/api.ts`: delete `startRuntimeGateway`, `stopRuntimeGateway`, and `restartRuntimeGateway` functions (currently at api.ts:309/317/325; the source-code paths are written as `/runtime/gateway/{start,stop,restart}` because `buildApiPath` auto-prefixes `/api`, so the external URL is `/api/runtime/gateway/{start,stop,restart}`); add `fetchCapabilities`, `fetchEndpoint`, `updateEndpoint(payload: {url, token, tlsVerify})` (token field is required — callers pass either the new plaintext token or the sentinel `__unchanged__` to ask the backend to resolve the active token before complete-object persistence), `testEndpoint`; narrow Settings types to match backend
- [x] 4.10 Mount `<FirstRunBanner />` and `<ModeBadge />` in the app shell (likely `App.tsx` or `Layout.tsx`)
- [x] 4.11 Update / add empty-state handling in panels that consume Gateway RPC: when API returns `code: "gateway_not_configured"`, render an empty-state message rather than an error toast
- [x] 4.12 Rewrite `SettingsPanel.test.tsx` as capability-gated: bundled-mode test verifies all endpoint fields read-only and accessToken read-only; remote-mode test verifies endpoint fields editable and Save flow; both verify appearance/notifications/pairedDevices remain editable
- [x] 4.13 Rewrite `GatewayPanel.test.tsx` as capability-gated: assert no Start/Stop/Restart buttons in any mode; assert supervisorState branching of field set
- [x] 4.14 Add E2E `bundled.spec.ts`: launch deck-go in bundled mode, verify mode badge, verify Settings endpoint section is read-only, verify GatewayPanel runtime shows PID/Ownership, verify chat round-trip works
- [x] 4.15 Add E2E `remote.spec.ts`: launch deck-go in remote first-run mode against mock Gateway, verify banner appears, configure endpoint via UI, verify reconnect, verify chat round-trip works

## 5. Local dev scripts, examples, docs

- [x] 5.1 Create `deck-go/.env.bundled.example` with all `RUNTIME_*` and `DECK_*` variables annotated with intent and example values (placeholder tokens, not real)
- [x] 5.2 Create `deck-go/.env.remote.example` with `RUNTIME_MODE=remote` and optional remote defaults
- [x] 5.3 Create `deck-go/scripts/dev/run-bundled.sh` that exports the bundled env vars (with self-generated dev token), then `exec deck-go`; do not modify the legacy `scripts/dev/deck-dev.sh`
- [x] 5.4 Create `deck-go/scripts/dev/run-remote.sh` that exports `RUNTIME_MODE=remote` with empty endpoint defaults and `exec deck-go`; document that the UI first-run wizard handles configuration
- [x] 5.5 Add an Architecture section to `deck-go/backend/README.md` describing the two-mode layout, facade interface, and capability flags
- [x] 5.6 Update `AGENTS.md` "Deck-go 开发环境（新主目标）" section to point to the new scripts and `.env` examples (the section already exists from commit `eed7ae1c8e`; this task only updates its sub-bullets to be concrete file paths now that they exist)
- [x] 5.7 Update `.agents/skills/deck-upstream-sync/SKILL.md`: replace any reference to `internal/runtime/supervisor.go` with `internal/runtime/bundled/supervisor.go` (and similarly for `preflight.go` and other relocated files); add a brief note that `runtime/config_sync*` is removed
- [x] 5.8 Verify the active-host-paths guard (`deck-go/scripts/check-active-host-paths.mjs`) still passes; it should be unaffected since we did not introduce frontend-next references

## 6. Cross-cutting verification

Each item is a single copy-runnable command (using deck-go's actual `Makefile` + `frontend/package.json` script set) + an explicit pass/fail criterion.

- [x] 6.1 Go backend tests: `cd deck-go && make backend-test` exits 0 (this expands to `cd backend && GOCACHE=/tmp/deck-go-buildcache GOMODCACHE=/tmp/deck-go-gomodcache GOPATH=/tmp/deck-go-gopath GOSUMDB=off go test ./...`). Includes relocated `internal/runtime/bundled/...`, new `internal/runtime/{envconf,state,shared,remote,facade,admin}/...`.
- [x] 6.2 Frontend unit tests: `cd deck-go/frontend && npm run test:deck-ui` exits 0; both `SettingsPanel.test.tsx` and `GatewayPanel.test.tsx` produce passing assertions for capability-gated rendering. (`test:deck-ui` is the only test script defined in `deck-go/frontend/package.json`; it shells into `../../scripts/run-vitest.mjs`.)
- [x] 6.3 Frontend build (subsumes type-check via the project's `tsc -b && vite build` chain): `cd deck-go/frontend && npm_config_cache=/tmp/deck-go-npm-cache npm run build` exits 0 (this is the same path `make frontend-build` takes); zero TypeScript errors. There is currently no separate `lint` script in `deck-go/frontend/package.json`; if Phase 4 adds one (e.g., oxlint), update this task to invoke it explicitly.
- [x] 6.4 E2E: launch the full stack via `deck-go/scripts/manage-local-stack.sh start` (the canonical local-stack manager used by `make stack-start`), then run `cd deck-go && npx playwright test bundled.spec.ts remote.spec.ts` (Playwright is invoked via npx so this works whether or not Playwright is hoisted into a workspace `package.json`); both spec files report PASS for every scenario. Tear down with `./scripts/manage-local-stack.sh stop`.
- [x] 6.5 Contract sync: `cd deck-go && make contracts-check` exits 0 (this expands to `node contracts/scripts/check-deck-api-generated.mjs`). Failure indicates `deck-api.generated.ts` / `deckapi.generated.go` need regeneration via `make contracts-sync` (= `node contracts/scripts/sync-deck-api-generated.mjs`).
- [x] 6.6 Breaking-removal HTTP probes (run with `RUNTIME_MODE=bundled` and `RUNTIME_MODE=remote` in turn against the running stack — note the actual external URL prefix is `/api`, since `server.go` mounts everything under `r.Route("/api", ...)`): `for path in /api/runtime/gateway/start /api/runtime/gateway/restart /api/runtime/gateway/stop; do code=$(curl -s -o /dev/null -w "%{http_code}" -X POST "http://127.0.0.1:${PORT}${path}"); [ "$code" = "404" ] || echo "FAIL $path got $code"; done` produces no FAIL output.
- [x] 6.7 Capability gating (frontend): `rg --type tsx --type ts 'capabilities\.mode\s*===\s*"(bundled|remote)"' deck-go/frontend/src --files-with-matches` SHALL only list paths matching `**/{ModeBadge,FirstRunBanner,*Banner,*Badge}.tsx` (the display-allowlisted files). Any other match is a violation.
- [x] 6.8 Token confidentiality (live dev run): start deck-go in `RUNTIME_MODE=remote` with a known fake token `T0KEN_TEST_42`; perform `PUT /api/runtime/endpoint`, `POST /api/runtime/endpoint:test`, `GET /api/runtime/endpoint`, `GET /api/settings`, `GET /api/runtime/gateway`; then `grep -RFn "T0KEN_TEST_42" deck-go/.local-stack-logs/ /tmp/deck-go-*.log` (or whatever paths `manage-local-stack.sh` writes its logs to — discover via `./scripts/manage-local-stack.sh status`) SHALL produce no matches; assertion: token byte sequence absent from all log files and from all HTTP response bodies (capture with `curl ... | grep -F T0KEN_TEST_42` returning empty).
- [x] 6.9 First-run UX (Playwright): launch with `RUNTIME_MODE=remote` and no `deck-state.json`; assertion sequence: `expect(page.locator('[data-testid="first-run-banner"]')).toBeVisible()`, `expect(page.locator('[data-testid="endpoint-section"]')).toHaveAttribute('aria-expanded','true')`, `expect(page.locator('[data-testid="empty-state-not-configured"]')).toBeVisible()` on each Gateway-data panel, `expect(page.locator('[role="alert"]')).toHaveCount(0)`.
- [x] 6.10 Backwards-incompatibility: place a legacy `openclaw.json` with a `managedGateway` block in the working directory and start deck-go with no `RUNTIME_MODE` env: `unset RUNTIME_MODE && cd deck-go/backend && go run ./cmd/deck-go 2>&1 | grep -E "RUNTIME_MODE.*(missing|required)"`; expected: process exits 64 and stderr contains a pointer to `.env.bundled.example` / `.env.remote.example`.
- [x] 6.11 Admin CLI surface: with the binary built (`cd deck-go/backend && go build ./cmd/deck-go`), `./deck-go admin status` prints the JSON document; `curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:${PORT}/admin/reload-runtime"` returns `404`; the admin socket file mode (`stat -f "%Mp%Lp" $SOCKET_PATH` macOS / `stat -c "%a" $SOCKET_PATH` Linux) is `660` when `RUNTIME_ADMIN_GROUP` resolves else `600`.
- [x] 6.12 Security enforcement: with `RUNTIME_BUNDLED_ENV_LD_PRELOAD=foo` set, `./deck-go` exits 64 and stderr contains the string `LD_PRELOAD`; with non-loopback bind + no TLS configuration, `./deck-go` (configured via env to bind `0.0.0.0:8080`) exits 64 and stderr contains `loopback`; `stat` of `deck-state.json` shows mode `600`; access-log capture for a `PUT /api/runtime/endpoint` carrying token `T0KEN_TEST_42` does not contain that token (assertion: `grep -F "T0KEN_TEST_42" $ACCESS_LOG | wc -l` is 0).
- [x] 6.13 Aggregate verify: `cd deck-go && make verify` exits 0 (this is the project's canonical pre-merge check: `contracts-check + host-check + backend-test + frontend-build`).
- [x] 6.14 Runtime gateway endpoint shape: probe `GET /api/runtime/gateway` against the running stack in each mode and assert the discriminated payload — bundled: `curl -s http://127.0.0.1:${PORT}/api/runtime/gateway | jq -e '.mode == "bundled" and has("pid") and has("ownershipState") and has("restartAttempts") and (has("lastConnectedAt") | not)'`; remote (configured): `... | jq -e '.mode == "remote" and has("lastConnectedAt") and has("latencyP50") and has("tlsVerified") and (has("pid") | not)'`; remote (first-run): `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:${PORT}/api/runtime/gateway` returns `503`.

## 7. Admin CLI (break-glass)

- [x] 7.1 Define admin verb surface in a new package `internal/admin/`: `Verb` enum (`reload-runtime`, `status`), `Handler` interface, `dispatch(ctx, facade, verb)` function — independent of any HTTP route
- [x] 7.2 Implement local-socket server (`internal/admin/socket_unix.go` + `socket_windows.go`) that serves admin verbs over a Unix-domain socket / named pipe; refuse to bind on TCP under any configuration
- [x] 7.3 On startup, create the admin socket at `RUNTIME_ADMIN_SOCKET` (default `/run/deck-go/admin.sock` POSIX, `\\.\pipe\deck-go-admin` Windows): if `RUNTIME_ADMIN_GROUP` is set and resolvable, use mode `0660` with that group ownership; otherwise fall back to mode **`0600` owner-only** (NOT `0660` with default group); create parent dir mode `0700`; clean up stale socket file from prior process; emit one INFO log line at boot describing the resolved mode + group
- [x] 7.4 Implement `cmd/deck-go/admin.go` (or extend existing CLI subcommand machinery) for `deck-go admin <verb>`: dial the socket, send the verb, print the single-line response, exit non-zero on failure
- [x] 7.5 Wire `reload-runtime` for bundled mode: call `bundled.Supervisor.ForceRespawn(ctx)` (new method that wraps the existing graceful-stop + start-with-current-cfg path)
- [x] 7.6 Wire `reload-runtime` for remote mode: drop the active RPC client and re-establish it against the currently active endpoint, applying the same drain helper from task 3.11
- [x] 7.7 Wire `status` with **read-only semantics** (the verb has no side effects on runtime state): returns a small JSON document `{mode, configured, endpointMutable, supervisorState, lastError, lastConnectedAt}` — same fields as `/api/runtime/capabilities` plus a couple of tail-state fields; the contract does NOT prescribe a separate read-only socket ACL (that is a deployment-level choice)
- [x] 7.8 Test matrix: socket file mode is `0660` after creation **when** `RUNTIME_ADMIN_GROUP` resolves; socket file mode falls back to `0600` **when** group is unset or unresolvable; `status` produces no observable side effect (state file unchanged, supervisor state unchanged, no client swap); HTTP probes against `/admin/*`, `/runtime/admin`, `/internal/admin/*` return 404; admin socket dial from a non-root user without group membership fails when fallback path is in effect
- [x] 7.9 Document the admin CLI in `deck-go/backend/README.md` (under "Operations") with examples for both modes

## 8. Security model enforcement

- [x] 8.1 Implement HTTP listener loopback-vs-TLS check at boot: if bind is not loopback and TLS is not configured, refuse to start with exit 64; loopback default in `.env.bundled.example` and `.env.remote.example`
- [x] 8.2 Implement URL scheme allowlist (`http`, `https`) for `RUNTIME_REMOTE_URL` env loading and PUT body validation; uniform `code: "invalid_url"` for all violations
- [x] 8.3 Implement file-mode enforcement for `deck-state.json`: on first write, set `0600` and parent dir `0700` POSIX; verify after-write mode in unit test; Windows ACL equivalent + audit
- [x] 8.4 Implement access-log redaction middleware via a `sensitiveBody` route tag; the v1 tagged set covers `PUT /api/runtime/endpoint`, **`POST /api/runtime/endpoint:test`** (also carries plaintext token in candidate-config body), and `PUT /api/settings`; the access-log writer SHALL omit request bodies for any tagged route; `Authorization` header always redacted globally; redaction SHALL be enforced via the route tag rather than per-route opt-in so that newly added candidate-bearing routes pick up redaction by tagging at registration time
- [x] 8.5 Implement WARN logging on any RPC issued against an endpoint with `tlsVerify: false`; log SHALL include URL but NOT token
- [x] 8.6 Add unit tests covering each `sensitiveBody` route (`PUT /api/runtime/endpoint`, `POST /api/runtime/endpoint:test`, `PUT /api/settings`): a request to each carrying a known plaintext token SHALL NOT surface that token in any log buffer (access log, error log, structured logger output)
- [x] 8.7 Add unit test: GET responses for `/api/runtime/endpoint`, `/api/runtime/gateway`, `/api/settings` over a recorded HTTP fixture do not contain the plaintext token under any field name
- [x] 8.8 Update `deck-go/backend/README.md` "Security" section listing the commitments above so they are discoverable for future reviewers
