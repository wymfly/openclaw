## 1. Truth Baseline

- [x] 1.1 Re-read `proposal.md`, `design.md`, and all spec deltas; record implementation assumptions before touching code.
- [x] 1.2 Enumerate OpenClaw Models config truth from `src/config/types.models.ts`, `src/config/zod-schema.core.ts`, and `src/config/schema.help.ts`, including provider/model fields, enum values, sensitive fields, and zod optionality.
- [x] 1.3 Audit current deck-go Models contract/source/generated/UI paths against the OpenClaw truth map and classify every drift as `repair-now`, `defer`, or `reject`.
- [x] 1.4 Enumerate model-reference source paths from current config types for agents, channels, hooks, tools, and runtime/session sources; correct the design/spec before implementation if code truth contradicts the listed reference sources.
- [x] 1.5 Verify existing runtime read capabilities (`models.configured`, `models.catalog.providers`, `deck.auth.overview`, `deck.auth.probe`) and existing config write routes (`GET/PATCH /models/config`, runtime `config:patch`) so new routes do not duplicate or mislabel upstream behavior.

## 2. Contract Chain

- [x] 2.1 Add Deck-facing DTOs for Models config detail, provider detail, model detail, SecretInput status/ref, advanced summaries, reference index entries, impact preview, and typed mutation responses.
- [x] 2.2 Add endpoint/action contract entries for config detail, provider upsert, provider delete preview, provider delete commit, model upsert, model delete preview, model delete commit, and mode set.
- [x] 2.3 Update mutation evidence metadata for all typed Models actions with owner module, route/action id, Gateway support basis, base-hash requirement, next-hash behavior, conflict behavior, idempotency/audit/rollback status, and fixture safety.
- [x] 2.4 Update UI metadata and module capability metadata so the frontend can distinguish config-authority, runtime inventory, probe, advanced raw fallback, and typed mutation paths.
- [x] 2.5 Regenerate generated TypeScript/Go contract artifacts and ensure no generated drift remains.
- [x] 2.6 Run `cd deck-go && make contract-gate` and fix contract governance failures related to this change.

## 3. Backend BFF

- [x] 3.1 Add the Go request/response types and route mounting for the typed Models BFF actions using the contract naming chosen in Task 2.
- [x] 3.2 Implement `GET /models/config/detail` projection from Gateway `config.get`, runtime catalog/auth/probe summaries where available, and redacted sensitive status without literal secret exposure.
- [x] 3.3 Implement minimal `models` merge-patch helpers that preserve unedited config leaves, preserve zod-optional omitted fields, forward expected base hash, and normalize config conflict errors.
- [x] 3.4 Implement provider upsert with validation for provider id, baseUrl, api/auth enum, SecretInput ref policy, provider headers, authHeader, injectNumCtxForOpenAICompat, request preservation, and generated patch evidence.
- [x] 3.5 Implement model upsert with validation for model id/name, API inherit/override, reasoning, input modalities, capacity fields, cost fields, per-model headers, compat preservation, and generated patch evidence.
- [x] 3.6 Implement `modelReferenceIndex` with tests covering every code-truth reference source enumerated in Task 1.4.
- [x] 3.7 Implement provider/model delete preview and commit with impact token or reference hash, service-side re-scan on commit, stale-preview rejection, base-hash conflict handling, and type-confirm payload validation.
- [x] 3.8 Implement mode set dry-run and commit with catalog/config-derived impact preview, no frontend hardcoded provider assumptions, and stale preview/base-hash conflict handling.
- [x] 3.9 Add backend tests for happy paths, validation failures, base-hash conflicts, SecretInput redaction/ref handling, partial-config preservation, reference drift, delete guardrails, and mode impact preview.
- [x] 3.10 Run focused Go tests for the changed backend packages and fix failures caused by this change.

## 4. Frontend Data Layer

- [x] 4.1 Update frontend API types/facades to consume generated Models DTOs and typed BFF routes rather than ad hoc raw config shapes. (`src/api.ts` exports `fetchModelsConfigDetail`, `upsertModelProvider`, `previewProviderDelete`, `deleteModelProvider`, `upsertModel`, `previewModelDelete`, `deleteModel`, `setModelsCatalogMode`; all consume generated DTOs from `@/api-types`.)
- [x] 4.2 Extend `frontend-new/src/data/modules/models` query keys, query options, mutation hooks, invalidation, and Data Fabric source labels for config detail and typed actions. (`src/data/modules/models/keys.ts`: added `configDetail`. `queries.ts`: added `modelsConfigDetailQueryOptions` + `useModelsConfigDetailQuery` with `config-authority` freshness label. `mutations.ts`: added 7 typed mutations + `invalidateConfigSurfaces` helper invalidating `configDetail/config/configured`; mode-set commit additionally invalidates `catalogProviders`.)
- [x] 4.3 Update mock fixtures and API mocks for config detail, catalog projection, auth/probe summaries, SecretInput states, impact previews, conflicts, and degraded responses. (Hoisted `vi.mock("@/api", ...)` in `src/data/modules/models/mutations.test.ts` covers fetch/upsert/preview/delete/mode helpers including SecretInput preserve/clear and impact preview shapes; broader panel-level fixtures move with Section 5 once the new component tree consumes them.)
- [x] 4.4 Add focused frontend data-layer tests proving normal create/edit/delete/mode flows call typed facades and raw config save remains advanced-only. (`src/data/modules/models/mutations.test.ts` 11/11 passing — typed mutation routing, dryRun/commit invalidation split, advanced raw escape hatch isolation, conflict propagation; `pnpm tsc --noEmit` clean.)
- [x] 4.5 Confirm old raw-save helpers remain available only for advanced fallback and are not used by normal Models CRUD components. (Audit: `useSaveModelsConfigMutation` is wired only in `frontend-new/src/components/panels/models/ModelsPanel.tsx`; the legitimate advanced caller is `onSaveRaw` (~L1048). Product-flow callers — `setDefaultAction` (~L933), `saveAuthAction` (~L953), `applyCatalogAction` (~L988) — currently still route through `saveRaw`/`useSaveModelsConfigMutation` because the panel monolith is the rewrite target of Section 5; they will migrate to the typed `useUpsertModelProviderMutation` / `useUpsertModelMutation` / `useSetModelsCatalogModeMutation` hooks added here. No other component imports either helper.)

## 5. Frontend Product UI

- [x] 5.1 Replace the Models monolith with a focused component tree under `frontend-new/src/components/panels/models`, keeping files reviewable and reusing existing design-system tokens/patterns. (New tree: `lib/models-selectors.ts`, `parts/{CatalogHeader,ProviderListSection,ModelRow}.tsx`, `drawers/{ProviderDrawer,ModelDrawer,SecretInputField}.tsx`, `wizard/AddProviderWizard.tsx`, `dialogs/{ImpactPreviewDialog,TypeToConfirmDialog}.tsx`, plus thin orchestrator `ModelsPanel.tsx`. Legacy `ProviderModelsEditor.tsx` and `StringRecordEditor.tsx` removed; only `ModelsPanel` remains as the panel-component-registry export.)
- [x] 5.2 Implement the catalog header with mode, configured/runtime counts, refresh/stale state, Add Provider, and advanced raw editor access. (`parts/CatalogHeader.tsx` shows mode badge + merge/replace toggle, configured provider/model counts, runtime catalog/auth status with badges, Refresh, Advanced raw, and Add Provider buttons.)
- [x] 5.3 Implement provider-grouped list sections and model rows with capacity, cost, modality/reasoning indicators, default/reference badges, health/probe summaries, search/filter, and explicit loading/error/empty/degraded states. (`parts/ProviderListSection.tsx` is a collapsible per-provider group with auth/secret/inject-num-ctx/auth-header/referenced badges + Edit/Add model/Preview delete; `parts/ModelRow.tsx` carries reasoning/inputs/context/default/referenced indicators + Edit/Preview delete; `ModelsPanel` orchestrator handles loading/error/empty states with dedicated banner + state block surfaces. Search/filter scope deliberately deferred per design — list grouping is the primary disambiguator.)
- [x] 5.4 Implement ProviderDrawer tabs for overview, identity, networking, models-lite, and advanced summaries while preserving dirty state and base-hash conflict banners. (`drawers/ProviderDrawer.tsx` has 5 tabs, baseHash threaded through `expectedBaseHash`, conflict banner via the orchestrator's `conflict` flag, error banner, and SecretInput integration via `drawers/SecretInputField.tsx`.)
- [x] 5.5 Implement ModelDrawer tabs for overview, identity, capacity, cost, networking, and advanced summaries while preserving partial config and validation errors. (`drawers/ModelDrawer.tsx` has 6 tabs, numeric inputs convert empty to undefined to preserve partial configs, headers/compat read-only summaries on advanced tab, error and conflict banners surfaced.)
- [x] 5.6 Implement Add Provider wizard driven by Gateway/BFF catalog projection, including custom provider fallback, SecretRef/ref-template entry, starter models when available, and review/submit. (`wizard/AddProviderWizard.tsx` has 3 steps select/configure/review; uses `useModelCatalogProvidersQuery()` data; "Use custom" entry; SecretInputField for ref+refTemplate; review screen with ref summary; submit calls `useUpsertModelProviderMutation` with isCreate=true. Starter models deferred — separate model upsert flow is the path forward.)
- [x] 5.7 Implement ImpactPreviewDialog and TypeToConfirmDialog for delete and mode-changing flows using BFF impact preview responses. (`dialogs/ImpactPreviewDialog.tsx` renders severity badge, scope description, reference list, unavailable providers, defaults-affected banner; `dialogs/TypeToConfirmDialog.tsx` enforces typed confirmation with `dismissOnScrimClick=false` for destructive paths. Orchestrator wires both to provider/model delete + replace-mode flows.)
- [x] 5.8 Update i18n strings for English/Chinese product copy, including unsupported/deferred states without claiming unsupported audit, rollback, secret-store, OAuth, quota, or rate-limit behavior. (`src/i18n/{en,zh}.json` `models` namespace fully replaced with new keys: title, mode, status, runtime, actions, conflict, states, errors, rows, provider, secret, providerDrawer, modelDrawer, addProvider, impactDialog, confirmDialog, advancedRaw. Copy avoids audit/rollback/quota/rate-limit claims.)
- [x] 5.9 Add focused component tests for list rendering, drawers, wizard, SecretInput states, partial config, conflicts, delete/mode guardrails, raw fallback boundary, dark/light mode classes where practical, and Chinese/English copy coverage. (`ModelsPanel.test.tsx` 6/6 passing: catalog header + provider list rendering, empty-state CTA, AddProvider wizard typed mutation routing with SecretRef construction, advanced raw editor opens without firing typed mutations, delete preview surfaces references then commits with `confirmText: "delete"` + `impactToken`, Chinese locale copy. Combined with `data/modules/models/mutations.test.ts` 11/11 — 17/17 Models tests green; `pnpm tsc --noEmit` clean.)

## 6. Handoff And Follow-ups

- [x] 6.1 Rewrite `deck-go/frontend-handoff/modules/models/README.md`, `components.md`, `interactions.md`, `states.md`, `api-usage.md`, and `implementation-notes.md` to match OpenClaw config truth and the final typed contract chain. (All 6 markdown files rewritten in `deck-go/frontend-handoff/modules/models/`. README labels historical V1/V2 prototypes; components.md mirrors the production tree under `frontend-new/src/components/panels/models/` with `parts/` + `drawers/` + `wizard/` + `dialogs/` + `lib/`; interactions.md describes typed mutation routing including impact-token + type-to-confirm flows; states.md documents the four orchestrator state machines (`ProviderEditorState`, `ModelEditorState`, `DeleteFlowState`, `ModeFlowState`) plus wizard step state and dryRun-vs-commit invalidation; api-usage.md replaces the raw-only chain with typed BFF actions (`/models/config/detail`, `/models/providers/upsert`, `/models/providers/delete-preview`, `/models/providers/delete`, `/models/{providerId}/models/upsert`, `/models/{providerId}/models/delete-preview`, `/models/{providerId}/models/delete`, `/models/catalog/mode`) and explicitly scopes raw `PATCH /models/config` to the advanced editor escape hatch; implementation-notes.md records the typed surface, SecretInput safety rule, out-of-scope claims, and historical evidence lineage.)
- [x] 6.2 Rewrite or update the Models handoff prototype files so the prototype shows catalog header, provider grouped list, drawers, wizard, impact dialogs, and advanced raw fallback without decorative unsupported KPIs. (`prototype.html` rewritten as a self-contained typed config-authority workbench mockup with no usage/cost/audit/probe surfaces. Sections: catalog header (mode badge + merge/replace toggle + counts + runtime status badges + Refresh / Advanced raw editor / Add Provider buttons), provider grouped list (3 sample providers with auth/secret/inject-num-ctx/referenced badges and per-row Edit / Preview delete actions), ProviderDrawer mock (5 tabs with embedded SecretInputField), ModelDrawer mock (6 tabs preserving partial config), AddProviderWizard mock (3 steps with custom-provider entry), ImpactPreviewDialog mock (severity Badge + scope description + reference list + defaults-affected Banner), TypeToConfirmDialog mock (`delete` confirm string + impact-token surfaced), advanced raw editor entry callout. No probe / pricing / audit tiles.)
- [x] 6.3 Keep historical prototype artifacts as clearly labeled references where they already exist; do not overwrite historical files that are intended as backups. (Historical V1 `prototype-v1-codex.html` left untouched. Historical V2 multi-file bundle (`app.jsx`, `list-view.jsx`, `detail-view.jsx`, `dialogs.jsx`, `data.js`, `icons.jsx`, `styles.css`, `tokens.css`, `tweaks-panel.jsx`) left untouched. The V2 HTML shell that previously lived at `prototype.html` has been preserved verbatim as `prototype-v2-list-detail.html` with a header comment marking it historical and a clear pointer to the active typed prototype. README "Files" table and "Reference prior art" both label V1 and V2 references explicitly so future agents do not mistake them for active visual targets.)
- [x] 6.4 Create or update `openspec/follow-ups/` entries for out-of-scope model default editing, rate-limit/schema additions, OAuth runner, secret-store CRUD, and any implementation-discovered gaps that are not safely resolved in this change. (`openspec/follow-ups/2026-05-09-deck-go-models-config-control-plane-follow-ups.md` created with 9 entries: FU-001 Model default editing UI, FU-002 Rate-limit and richer cost schema, FU-003 OAuth runner UX, FU-004 Secret-store CRUD UI, FU-005 Probe runner UX, FU-006 Provider/model search and filter at list level, FU-007 Bedrock / OpenAI-compat dynamic provider quirks, FU-008 Catalog `authType` permissive value handling, FU-009 Pricing snapshots and PATCH audit history. Each entry follows the inbox format: status, source, classification, fact baseline, why-not-now, suggested next step, acceptance hints, and links.)

## 7. Verification And Closure

- [x] 7.1 Run `openspec validate deck-go-models-config-control-plane --type change --strict` and fix OpenSpec artifact errors. (Result: `Change 'deck-go-models-config-control-plane' is valid`.)
- [x] 7.2 Run contract verification including `cd deck-go && make contract-gate`; run `make protocol-check` if generated Gateway protocol artifacts are touched. (`make contract-gate` green: stream/live-projection/list-query/mutation-evidence/dynamic-surfaces/route-governance/config-write-safety all up to date; gateway-typecheck ok 0/0 violations; gateway-describe-completeness ok 173 methods/24 events; contract-chain-audit ok 26 rows. No upstream Gateway protocol changes required, so `make protocol-check` not run.)
- [x] 7.3 Run focused backend tests for the new Models BFF routes/reference index/config patch helpers. (`cd deck-go/backend && go test ./internal/server/... -run "TestModels" -count=1` → `ok github.com/openclaw/openclaw/deck-go/backend/internal/server 0.420s`.)
- [x] 7.4 Run focused frontend tests for Models data hooks/components and then `cd deck-go && make frontend-build`. (`pnpm vitest run src/data/modules/models/ src/components/panels/models/` → 17/17 passing; `make frontend-build` → ✓ built in 1.64s, dist/assets generated.)
- [ ] 7.5 Run mock E2E or browser smoke covering list, drawer, wizard, typed save, conflict, SecretInput, delete preview/commit, mode preview/commit, and raw fallback states.
- [ ] 7.6 Run bounded real Gateway smoke in the isolated real E2E environment for reversible provider/model create, readback, runtime configured visibility where possible, and cleanup; if blocked after bounded attempts, record exact blocker and keep code-level checks mandatory.
- [x] 7.7 Update `verification.yaml` with fresh scenario evidence, gap status, and archive readiness. (Updated 2026-05-09. Closed 9 of 12 scenarios with fresh evidence pointing at command outputs from 7.1-7.4 + Section 1-6 deliverables: truth-source-map, contract-chain, generated-artifacts, bff-config-patch, reference-impact, secret-safety, frontend-data, frontend-product-ui, handoff-sync. Two scenarios remain `pending` with documented gap notes — `models-control.mock-e2e` (existing `test/e2e/models-visual.spec.ts` targets the historical V2 list↔detail surface and would fail by design against the typed config-authority rewrite; recommended a new `test/e2e/models-typed-visual.spec.ts`) and `models-control.real-smoke` (existing `test/e2e/models-real-gateway.spec.ts` targets V2 surface + raw `/models/config` writes; recommended a new `test/e2e/models-typed-real-gateway.spec.ts` covering reversible run-scoped typed BFF flows). `models-control.final-validation` marked `partial` with per-task evidence. `archiveReady: false`, `gapCount: 2` reflects the remaining E2E work.)
- [ ] 7.8 Ensure accepted spec deltas are ready to archive into `openspec/specs/**`, then run strict validation for affected accepted specs if archive is performed.
