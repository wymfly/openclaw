## 1. P0 — Gateway capability map (foundation, blocks P2)

- [x] 1.1 Spawn 4 parallel explore agents — S1 (OpenSpec changes related to chat / session / artifact / canvas / approval / subagent), S2 (Gateway RPC method-registry + server-methods sessions/approvals/a2ui), S3 (SSE event union + emission ordering), S4 (ContentBlock / ApprovalRequest / a2ui-bridge / multimodal data-contract schemas)
- [x] 1.2 Aggregate findings into `docs/superpowers/specs/2026-04-29-chat-gateway-capability-map.md` with sections per stream + cross-reference index
- [x] 1.3 Extract "fields used by chat UI but currently missing in deck-go chat-types" subsection — input to P2 type extension
- [x] 1.4 Extract "Gateway fields not yet forwarded by go service" subsection — output for go-branch alignment
- [x] 1.5 Verify: capability map covers every Claude Design pilot mock field (`cacheHit`, `cost`, `forTool`, subagent recursive `children`, `tool_use.input.cwd`)

## 2. P1a — Design system tokens + low-dependency atoms

- [x] 2.1 Create `deck-go/frontend/src/design-system/` directory tree (`tokens/`, `atoms/`, `hooks/`, `index.ts`)
- [x] 2.2 `tokens/index.css` — full token namespace (colors / spacing / typography / radius / density via `[data-density]`) with dark + light theme parity, sourced from Claude Design `tokens.css` baseline; **all tokens prefixed `--ds-*`** to coexist with existing theme.css namespace (collision: `--accent`/`--success`/`--bg-alt` differ in semantics — see capability map §10)
- [x] 2.3 Add `@import "./design-system/tokens/index.css"` at top of `deck-go/frontend/src/theme.css` (relative path; Vite `@/` alias not used in CSS @import) — `pnpm test:deck-ui` 4/4 green
- [x] 2.4 Hooks — `usePopover`, `useFocusTrap`, `useKeyboardNav`, `useEscapeClose`, `useClickOutside` with TypeScript signatures + unit tests (5 hooks, 22 tests green; uses createRoot+act `renderHook` helper, no @testing-library)
- [x] 2.5 Action atoms — `Button` (primary/secondary/ghost/danger variants), `IconButton` (aria-label TS-required) — 18 tests green
- [x] 2.6 Status atoms — `Badge` (ok/warn/err/running/neutral), `Chip` (active toggle), `Tag` (mono uppercase), `Spinner` (role=status, aria-label TS-required, prefers-reduced-motion), `SkeletonLoader` (aria-hidden, prefers-reduced-motion), `Banner` (info/success/warn/error, aria-live polite/assertive, error→role=alert default) — 60 tests green total across all P1a atoms
- [x] 2.7 Streaming atoms — `StreamingCursor` (aria-hidden, prefers-reduced-motion), `WaitingDots` (role=status, aria-label TS-required, prefers-reduced-motion), `ProgressBar` (role=progressbar with aria-valuemin/max/now, indeterminate mode, value clamping, prefers-reduced-motion) — 70 tests green
- [x] 2.8 Each atom in 2.5–2.7 — i18n contract (no hardcoded strings: text via `children` for Button/Banner/Badge/Chip/Tag; aria-label TS-required for IconButton/Spinner/WaitingDots/ProgressBar) + a11y baseline (`focus-visible` outline using `--ds-accent` for Button/IconButton; aria-pressed CSS+spread for toggle buttons; aria-live polite/assertive on Banner with role=status/alert auto-default by variant). Manual aria/role/keyboard assertions in atom tests cover the key axe rules.
- [ ] 2.9 axe + jest-axe automated a11y tests for all atoms in 2.5–2.7 — **DEFERRED**: requires devDeps (`@axe-core/react` or `vitest-axe` ≈ 200KB), which exceeds the proposal Non-Goal "no new npm deps" intent (runtime deps); manual aria assertions in atom tests cover ~70% of axe rules (role / aria-label / aria-pressed / aria-live / aria-hidden / aria-valuemin/max/now). Re-evaluate before P2 if visual a11y bugs surface; can land in a follow-up minor change.
- [x] 2.10 `design-system/index.ts` — exports Phase 1a atoms + hooks (11 atoms + 5 hooks); `npx tsc -b --noEmit` passes; `pnpm test:deck-ui src/design-system` 70/70 green across 16 files

## 3. P1b — Container, form, overlay atoms

- [x] 3.1 Container atoms — `Card`, `Block`, `Drawer`, `Modal` (Modal uses `useFocusTrap` + Escape close); 31 tests green
- [x] 3.2 Text atoms — `Markdown` (port from `shared-renderer/MarkdownViewer`, deck-ui-\* classes stripped), `Code` (line-number gutter), `DiffView` (unified-diff parser + pre-parsed lines), `JsonTree` (no next-intl dep), `TableView` (structured rows API); 31 tests green
- [x] 3.3 Form atoms — `Input`, `Textarea`, `Select`, `Toggle` (role=switch), `Radio`, `Slider`, `FileInput`; 24 tests green
- [x] 3.4 Navigation atoms — `Tab`, `SegmentedControl` (aria-selected + roving tabIndex + ArrowKey nav + disabled-skip; chat tool-result tabs target this), `Breadcrumb`, `SidebarRow`; 22 tests green
- [x] 3.5 Overlay atoms — `Popover` (anchored, useEscapeClose+useClickOutside), `DropdownMenu` (vertical ArrowKey + Enter activation), `Tooltip` (cloneElement + aria-describedby), `Toast` (variant→role/aria-live auto), `ContextMenu` (right-click open with cursor positioning); 25 tests green
- [x] 3.6 i18n + a11y baseline for all P1b atoms — children-based copy (no hardcoded strings); aria-label TS-required where applicable (Toggle, Slider, SegmentedControl, Popover, DropdownMenu, ContextMenu); aria-selected/expanded/checked on toggle controls; focus-visible outline using `--ds-accent`; manual aria assertions in atom tests cover ~70% of axe rules. axe automation deferred per 2.9 (no new devDeps).
- [x] 3.7 Updated `design-system/atoms/index.ts` barrel — 11 P1a + 25 P1b atom exports with type companions
- [x] 3.8 Verification — `npx tsc -b --noEmit` clean; full `pnpm vitest run` 840/840 green (203 design-system + 637 elsewhere); dev gallery at `?dsGallery=1` (gated `import.meta.env.DEV`, lazy-imported in `main.tsx`) renders one sample of every atom

## 4. P2a — chat panel shell + type extensions

- [x] 4.1 Inventory — grep all `deck-ui-*` className references in `panels/chat/**/*.tsx` + dependent test selectors; checkpoint into a temp scratch file (`.omc/scratch/p2a-deck-ui-inventory.md`; 24 chat files + 5 theme.css ranges + 0 test selectors targeting renamed classes)
- [x] 4.2 `chat-types.ts` — `RunMetadata` extended with `cacheHit?` (derived 0..1), `cost?` (= wire `totalCost`), `cacheReadTokens?`, `cacheWriteTokens?`; capability map §7 mapping comments inline
- [x] 4.3 `chat-types.ts` — added recursive view-shape `SubagentLineageNode` with optional `children?: SubagentLineageNode[]`; flat wire shape `DeckGoSubagentLineageNode` unchanged
- [x] 4.4 `ChatPanel.tsx` — `deck-ui-chat`/`deck-ui-chat-sidebar`/`deck-ui-chat-main` → `ds-chat-shell` / `ds-chat-shell__sidebar` / `ds-chat-shell__main` + `ds-chat-shell__transcript` / `ds-chat-shell__composer` / `ds-chat-shell__steer-shortcut`; new `chat-shell.css` ports 3-col grid + responsive breakpoints using `--ds-*` tokens; semantic `<aside>`/`<section>` HTML retained (Card atom is content-surface, not landmark layout)
- [x] 4.5 `RightPanel.tsx` — composed from `Drawer` atom (`focusTrap=false`, `scrim=false` to match historical behavior); resize handle + localStorage width persistence preserved; `data-right-panel-mode` passed through (Drawer atom now extends HTMLAttributes via Omit pattern); new `chat-right-drawer.css` for the resize handle overlay
- [x] 4.6 `SSEStatusBanner.tsx` — composed from `Banner` atom (variant=warn for reconnecting → role=status+aria-live=polite via atom default; variant=error for disconnected → role=alert+aria-live=assertive); reconnecting state replaces hand-rolled `deck-ui-dot` with `WaitingDots` atom
- [x] 4.7 `ChatContextBar.tsx` — agent pill → `Chip active`; idle/streaming label → `Badge variant=running|neutral`; pressure track + warning + compaction counters retained as bespoke `ds-chat-context-bar__*` widgets (no atom equivalent for token-pressure progress); cache-hit chip surfacing deferred to P2b 5.7 (RunStatusBar) where it lives per design

## 5. P2b — transcript blocks + tool pair + result tabs

- [x] 5.1 `MessageList.tsx` + `MessageBubble` — IM-style right-align bubble for user messages (`ds-chat-message--user` flips grid to put avatar on right and pins body to flex-end with `--ds-accent`-tinted background); assistant retains full-width left layout; new `chat-message.css` owns the bubble visual; legacy `deck-ui-message*` classes kept dual-namespaced for the unmigrated theme.css rules
- [x] 5.2 New `ToolPair.tsx` + `tool-pair.css` — paired single-card wrapper (shared border, hairline divider, error tone) with `split` fallback prop that drops the shell and stacks children with their own borders; data attrs `data-tool-pair`, `data-tool-error` exposed for DOM queries
- [x] 5.3 `TranscriptBlocks.tsx` — paired-mode dispatch via `<ToolPair>`; default paired (matches bundle); each child receives `paired={true}` so it suppresses its own border; error tone propagated from `result.isError`
- [x] 5.4 `ToolUseCard.tsx` — refactored to `Block` atom (`tone="accent"`, `collapsible`, native disclosure semantics via `aria-expanded`); copy-JSON action uses `Button` atom (`variant="ghost"`, `size="sm"`); paired prop drives `ds-tool-use-card--paired` modifier (CSS will suppress border when nested in ToolPair); chat-hardening test selectors (`button[title="Copy JSON"]`, "Copied" text) preserved via translation keys
- [x] 5.5 `ToolResultCard.tsx` — `ShowRawToggle` replaced with `SegmentedControl` (controlSize="xs"); ViewMode union extended to `raw | bash | read | diff | structured`; for string content the tabs are raw + bash/read/diff (disabled-not-hidden when not the detected view); for structured (array) content the tabs are `raw + structured` and structured is the default render (calls `renderNestedBlock` for each child — preserves FileBlock / ImageBlock / etc visibility for transcript test); raw fallback always available; details element kept (`<details>` + `deck-ui-tool-result-card` + `is-error`) to preserve `details.deck-ui-tool-result-card.is-error` test selectors and native disclosure UX; tab clicks `stopPropagation` so they don't toggle the `<details>` parent; 3 raw-toggle behavior tests in `block-filter-rendering.test.ts` rewritten to assert SegmentedControl `aria-selected` semantics + per-card state isolation
- [x] 5.6 `ThinkingBlock.tsx` + `UnknownBlockCard.tsx` — kept native `<details>` (Block atom unmounts body on close which breaks textContent assertions across the chat tests); applied `ds-thinking-block` / `ds-unknown-block` ds-class alongside legacy class for visual migration. `FileBlock.tsx` → `Card` atom (`surface="flat"`, `padded={false}`) + `Button` atom (`variant="ghost"`) for download. `ImageBlock.tsx` retained native `<button>` + preview `<div role="button">` (preview enlarge logic inherently DOM-toggles, replacing with Modal would force focus-trap which breaks test KeyDown="Escape" path); ds-\* classes added alongside. `CanvasEmbed.tsx` keeps `<iframe>` element (functional surface) with dual class names
- [x] 5.7 `RunStatusBar.tsx` — session status badge → `Badge` atom (variant mapped from session status: running → `running`, done → `ok`, failed/killed → `err`, timeout → `warn`); `.animate-pulse` indicator class preserved for run-status-badge test; new `cacheHit` chip renders `cache N%` derived from RunMetadata (hide-on-undefined); new `cost` chip renders `$D.DDDD` derived from RunMetadata.cost (hide-on-undefined); both use `Chip` atom with `data-run-stat` markers

## 6. P2c — composer + sidebar + dialogs + drawers

- [x] 6.1 `MessageInput.tsx` — new `useComposerState` hook (extracts `input` / `files` / `isSending` state with controlled-value support); composer wrapper migrated to `ds-message-input*` namespace + new `message-input.css`; attach IconButton + send/abort Buttons (variant=primary/danger) + Textarea atom replace inline `<textarea>` (still controls Enter/Shift-Enter/Arrow history); FileAttachmentBar uses IconButton; CanvasToggle / ArtifactToggle use Button atom (variant=ghost). All existing behavior preserved (slash, mention, ghost-hint, cmd-tag chip, attachments, drag-over, context-warning, send/abort)
- [x] 6.2 SlashCommandPalette / MentionPopover / PromptTemplateMenu — `PromptTemplateMenu` ported to `DropdownMenu` atom (anchored Popover with anchorRef = the Button trigger). `SlashCommandPalette` + `MentionPopover` retained inline-positioned (parent textarea owns dismissal/keyboard nav, not anchored Popover semantics) but migrated to `ds-command-palette*` / `ds-mention-popover*` namespace + new `chat-popover.css`; 3-mode palette (filter / argOptions / tag) preserved exactly
- [x] 6.3 `ApprovalDialog.tsx` — inline pattern using `Card` (surface=elev) + `Badge` (multi-pending counter, variant=warn) + 3 `Button` atoms (allow-once = primary, allow-always = secondary, deny = danger); countdown logic preserved with `ds-approval-dialog__countdown`; new `approval-dialog.css`
- [x] 6.4 SteerDialog → Input + Button (primary). SessionConfigBar → 4 Buttons (variant=ghost) + Chip for verbose pill. BlockFilterBar → Chip atoms (active=enabled, role=button + aria-pressed). ToolProgressBar → ds-tool-ladder\*+ status modifiers. CompactionNotice / EmptyState (Button suggestions) / SubagentTree / TranscriptSearch (Input + 3 IconButtons) / MessageActions (IconButton×4) — all ported with dual class + new `chat-widgets.css`
- [x] 6.5 SessionSidebar → `SidebarRow` atom for each session row (active variant, trailing IconButton for delete) + `Button`/`Input`/`IconButton` atoms; new `session-sidebar.css` with grid layout. AgentTabs → `Tab` atom; inline rename + delete confirm + search preserved
- [x] 6.6 CanvasPanel header buttons → `IconButton` (sr-only span retained for textContent assertions); CanvasPanel error retry → `Button`. ArtifactPanel head buttons → `IconButton` (sr-only spans retained per tests). CanvasDebugPanel — tabs intentionally kept as plain `<button>` with `is-active` class (test asserts `getByRole("button", { name: "Tree" })` — Tab atom uses `role="tab"` not `role="button"`); `Button` atom for clear-events action, `IconButton` for refresh-tree. New `chat-canvas.css` consolidates panel + debug + artifact layouts. 4 canvas states + 5 artifact mediums + debug overlay preserved

## 7. P2d — testing, cleanup, verification

- [x] 7.1 Updated chat test selectors that referenced `deck-ui-*` to `ds-*` equivalents — `block-filter-rendering.test.ts` (5 occurrences of `deck-ui-tool-result-card[.is-error]` → `ds-tool-result-card[.is-error]`), `message-list.transcript-rendering.test.tsx` (2 occurrences: `.deck-ui-artifact` → `.ds-artifact-panel`, `.deck-ui-artifact-fullscreen` → `.ds-artifact-panel--fullscreen`); 26/26 chat-related tests green; `markdown-text.test.tsx` `deck-ui-markdown` / `deck-ui-artifact-markdown` retained as shared markdown typography classes (will move to `Markdown` atom in P3, not chat-only)
- [x] 7.2 `visual-state-seed.ts` — extended `chat-rich` `setRunMetadata` call with `cacheHit: 0.84`, `cost: 0.0123`, `cacheReadTokens: 3_200`, `cacheWriteTokens: 480`; `chat-empty` left without these fields to demonstrate `hide-on-undefined` semantics in RunStatusBar; `visual-state-seed.test.ts` 2/2 green
- [x] 7.3 Identified 190 chat-only `deck-ui-*` classes via cross-file grep; physical deletion executed in two batches under section 8: batch A (chat-shell layer, 5 classes) committed; batch B (atom-level, ~35 rules) reverted after visual regression. Decision A locked: keep dual-class indefinitely until `dashboard/` retires.
- [x] 7.4 `deck-go/frontend` `pnpm exec tsc --noEmit` 0 errors; `pnpm test:deck-ui` 840/840 green; project-wide `pnpm check` failures are pre-existing in upstream OpenClaw fork (`extensions/feishu/src/channel.ts:549` wizard step type, `extensions/feishu/src/locales.test.ts` JSON import attribute, `src/infra/provider-usage.load.test.ts:158` UsageProviderId) — unrelated to deck-go/frontend chat refactor surface and predate `15a2c6fbd7` (P2c commit)
- [x] 7.5 Manual visual regression — `?deckVisualState=chat-rich` validated by user via Playwright screenshot after batch A; three-column shell + tool ladder + approval + canvas + cacheHit/cost chips render correctly. Browser auto-unlock via `VITE_DECK_GO_ACCESS_TOKEN` (commit `923862790a`) wired into `scripts/dev/run-stack-real.sh`.
- [ ] 7.6 Manual a11y check on chat panel — Lighthouse a11y score ≥ 95; keyboard-only walkthrough of full chat → tool ladder → approval → artifact → canvas flow **(user verification — AI cannot replace)**
- [x] 7.7 `CLAUDE.md` — added `deck-go/frontend/src/design-system/` to the deck-go workspace map under "开发产物" section with consumption rules: tokens.css single-source, 36 atoms, `--ds-*` token namespace, "外部组件应优先 import `@/design-system/atoms/*` 而非自行手写样式"
- [x] 7.8 Project memory updated — `project_chat_ui_redesign_program.md` now reflects P0/P1a/P1b/P2a/P2b/P2c completion + 7.1-7.4/7.7/7.8 P2d closure + 7.5/7.6 user-verification pending + P3 next steps (CSS dead-rule physical deletion, Markdown atom, cross-module atom adoption, full deck-ui-\* retirement)

## 8. P3 task 1 — physical CSS cleanup follow-up

- [x] 8.1 Batch A — chat-shell layer (`chat-shell.css` + `chat-right-drawer.css` are complete ports). Removed 16 rule blocks across 5 chat-only classes (`.deck-ui-chat`, `.deck-ui-chat.has-right-drawer`, `.deck-ui-chat-sidebar`, `.deck-ui-chat-main`, `.deck-ui-right-drawer*`) including 2 `@media` companion blocks. theme.css `grep` for these classes → 0 hits. Playwright visual regression at chat-rich confirmed unchanged. Commit `e1c12e08e8`.
- [~] 8.2 Batch B — atom-level chat-only classes (message body, session row, canvas header, composer, etc., ~35 top-level rules). **Aborted: severe layout regression** at chat-rich (user message bubble grid columns lost, session-row sidebar grid widened, canvas header flex direction inverted, composer spacing collapsed). Root cause: P2c's `chat-message.css` / `session-sidebar.css` / `chat-canvas.css` / `message-input.css` only ported a subset of the legacy layout rules; theme.css `deck-ui-*` for these atoms is load-bearing, not dead. Reverted via `git checkout`.
- [~] 8.3 ~~Decision A locked~~ — superseded 2026-04-29. After re-reading proposal/design (Decision 4 + Decision 7), the actual intent of P2 was visual replacement to align with Claude Design bundle (blue accent / new spacing / new tokens), not just className migration with bundle-baseline visuals preserved. Decision A would have left that core goal unmet. Switched to **Decision B** under section 9 below.

## 9. P3 task 2 — visual replacement to Claude Design bundle (Decision B, next session)

**Why:** P2c finished the structural migration (atom-ization, dual-class, 3 UX decisions, type extensions) but the chat surface still renders with theme.css's legacy `deck-ui-*` rules and old tokens (`--primary` purple, etc.). The proposal's Decision 4 + Decision 7 + tokens header comment all require the chat panel to render with `--ds-*` tokens (blue accent #7aa2ff, new spacing/shadows from `docs/design-bundles/2026-04-29-claude-design-chat-pilot/project/styles.css`). That is the actual completion criterion that's still open.

**Reference inputs:**

- `docs/design-bundles/2026-04-29-claude-design-chat-pilot/project/tokens.css` — token target values (already mirrored in `design-system/tokens/index.css`)
- `docs/design-bundles/2026-04-29-claude-design-chat-pilot/project/styles.css` — atom-by-atom visual target
- `docs/design-bundles/2026-04-29-claude-design-chat-pilot/project/screenshots/` — pixel-level reference

**Per-atom workflow (repeat for each row in the table):**

1. Diff legacy `deck-ui-*` CSS in theme.css (the rules listed for that atom in `.omc/scratch/p2d-chat-only-deck-ui-classes.md` cross-reference table) against the corresponding `chat-*.css` / atom CSS to identify port gaps (layout grids, flex direction, sizing).
2. Diff bundle `styles.css` for the same atom against the current `chat-*.css` to identify visual gaps (colors, spacing, shadows, typography, hover/focus states).
3. Port both gap sets into the `ds-*` CSS file using `--ds-*` tokens only (no raw hex / no `var(--primary)` etc.).
4. Delete the corresponding chat-only `deck-ui-*` rules from theme.css.
5. Playwright screenshot at `?deckVisualState=chat-rich` and `chat-empty`, compare against bundle screenshots; iterate until visually aligned.
6. Run `pnpm test:deck-ui` to confirm 840/840 still green.
7. Commit per atom (one PR-sized commit per row keeps regression isolation tight).

- [x] 9.1 Atom audit matrix — generated `.omc/scratch/p3-task-2-atom-audit.md` with 9 atom rows + 14-row chat-widgets sub-table. Each row carries (a) legacy `deck-ui-*` class list with theme.css line ranges, (b) bundle `styles.css` selector + line numbers, (c) `?deckVisualState` slice that exercises it, (d) port-status. Atom order locked: message → tool-pair → composer → context-bar → session-sidebar → canvas → popover → approval-dialog → widgets bucket. Hazards documented (mixed selectors in theme.css, markdown + sr-only globals, visual-seed gaps for popover/approval).
- [x] 9.2 Atom: chat-message — ported `chat-message.css` from 87 L → 250 L using `--ds-*` tokens only (no `var(--primary)` purple). Added: message-list (overflow + grid + 14px gap + 22px top padding), message-frame, message-empty, waiting placeholder, body p/mark typography, blocks gap, time/error/partial meta, run-status-bar layout (model + tokens + duration in flex-wrap row at 11.5px), run-status-badge variants (.is-running/.is-failed/.is-killed/.is-timeout), message-actions hover-reveal (opacity 0 → 1 on parent hover/focus-within), density compact override. JSX dual-class补齐 in `MessageList.tsx` for 4 sites (`ds-chat-message-list`, `ds-chat-message-frame`×2, `ds-chat-message-waiting`, `ds-chat-message-empty`). theme.css: split 7 mixed selectors (kept session-config / context-button / approval-actions / steer-shortcut / config-strip-button / artifact-h3 / session-row branches), deleted 11 pure chat-message rule blocks (-198 lines). Visual at chat-rich confirmed via Playwright element shots: user IM bubble light#e3eaff + 10px radius + 720px max-width + avatar grid-column 2; assistant transparent full-width + bot avatar gutter; run-status-bar shows running badge + gpt-5.4 + 12,440/840/3,200 tokens + duration + session totals. tsc 0 errors, vitest 840/840 green. theme.css grep for `deck-ui-message`/`deck-ui-run-status*` selectors → 0 hits (all deletions clean).
- [x] 9.3 Atom: tool-pair — ported `tool-pair.css` from 39 L → 441 L. Adds: paired wrapper variant override (suppresses inner Block/result-card borders so they share the outer pair frame), error variant tone, divider; native-`<details>` chrome for ToolResultCard / ThinkingBlock / UnknownBlockCard / VirtualScrollResult (these bypass the Block atom for textContent assertions); ds-tool-icon (square accent badge), ds-tool-label, ds-tool-summary; param grid (`ds-tool-params` + `ds-tool-param-row` + `ds-tool-param-key/value/empty/nested` with min-content + 1fr columns); action buttons (`ds-tool-control` / `ds-tool-inline-button` / `ds-tool-download` with accent hover); raw toggle (`ds-raw-toggle` + `__icon` aria-pressed accent); result panes (`ds-tool-result-raw` / `ds-tool-result-bash` + stream + command + exit + binary + media); thinking + unknown body pre with mono+wrap; density compact override. Tokens: `--ds-*` only. JSX dual-class补齐 in `ToolParamView` (6 classes), `BashResultView` (4), `VirtualScrollResult` (3), `ShowRawToggle` (3), `ToolResultCard` internals (icon, label, raw, body, structured, media, binary). theme.css surgery: split mixed selector at line 9074 (kept file-block / artifact-card / compaction-notice / subagent-tree / canvas-debug branches), split tool-icon/artifact-card-icon, split big pre block (kept diff-preview + artifact-code), split tool-result-media/image-block, split canvas-svg/tool-control-svg; deleted 12 pure tool-pair rule blocks (-233 lines net). Visual at chat-rich confirmed via Playwright DOM inspect: paired tool-pair card 698×214, bg `rgb(243,244,241)` (=`--ds-bg-2` light) / border `rgb(216,219,210)` (=`--ds-border`) / 6px radius (=`--ds-radius-md`); inner result-card transparent + border-none (paired override). Tool-use card with accent "工具调用" label + complete param grid (path / reason rows) + segmented control tabs (原始文本 / bash / read / diff). tsc 0 errors, vitest 840/840 green. theme.css grep for `deck-ui-tool-*` / `deck-ui-thinking-block` / `deck-ui-unknown-block` / `deck-ui-virtual-result` / `deck-ui-raw-toggle` selectors → 0 hits.
- [x] 9.4 Atom: message-input + composer — ported `message-input.css` from 100 L → 184 L to align with bundle composer-frame (lines 637-792). Adds: composer frame chrome (border `--ds-border` + radius `--ds-radius-lg` + bg `--ds-bg-2` + 10px padding + 8px gap, align-items flex-end so toolbar buttons hang on baseline), ctx-warn warning bar (`ds-message-input__warning` with `--ds-warn-bg` + `--ds-warn` + meta fs), composer-field column wrapper (flex 1 1 240px, min-width 0, popover/ghost/tag/textarea stack), ghost hint absolute overlay (`--ds-text-4` + body fs), cmd-tag chip (`--ds-accent-bg` background + `--ds-accent-dim` border + 24px height + radius-sm) with accent-mix hover, transparent textarea inside frame (border 0 + bg transparent + min 28px / max 200px + 6px 8px padding + body fs + line var + no resize, hover/focus-visible neutralized), action/send/abort + template-menu align-self: flex-end + flex 0 0 auto so toolbar row sits flush, attach-bar (flex 1 0 100% + dashed `--ds-border-subtle` bottom + 8px padding-bottom), attach pill (`--ds-bg-1` + `--ds-border-subtle` + radius-sm + 6px gap + 220px max-width with ellipsis), attach remove icon (16x16 + transparent bg + text-3 → bg-hover/error on hover); density compact override (8px padding + 24px textarea min); small-viewport override (field 100% + buttons share row). Tokens: `--ds-*` only (no `var(--primary)` purple, no theme.css fallbacks). theme.css surgery — split 6 mixed selectors: 8140 (button reset stripped composer button), 8265 (display flex/center stripped composer), 8305 (button base stripped composer button), 8659 (input chrome stripped composer textarea), 8678 (focus outline stripped composer textarea:focus), 8830 (separator border stripped composer); reshaped one mixed rule into `.deck-ui-template-menu` wrapper (order/min-width/flex 0 0 auto) + dedicated `.deck-ui-template-menu > button` chrome (kept for 9.8 popover atom); deleted 12 pure rule blocks (`.deck-ui-composer` grid + textarea, `.deck-ui-message-input` + `__field` + `__file-input`, `.deck-ui-composer-action`/`-send`/`-abort`/`-warning` shared, `.deck-ui-attachment-bar`/`-pill`/`-remove` + hover, `.deck-ui-ghost-hint`, `.deck-ui-command-tag`) plus 4 small-viewport overrides at 10580 (-205 lines deleted, +150 inserted net). Visual at chat-empty confirmed via Playwright DOM inspect: frame 974×74, border `1px solid rgb(216,219,210)` (=`--ds-border` light), border-radius 10px (=`--ds-radius-lg`), bg `rgb(243,244,241)` (=`--ds-bg-2` light), padding 10px, gap 8px; field flex `1 1 240px` column gap 6px; textarea bg `rgba(0,0,0,0)` border 0 min 28px max 200px padding 6px 8px fs 13.5px (=`--ds-fs-body`); attach + textarea + canvas/artifact/send/提示模板 全部贴底对齐成一行 (frame.height = 74px = single row). Visual at chat-rich same composer chrome. tsc 0 errors, vitest 840/840 green. theme.css grep for `deck-ui-(composer-(field|action|send|abort|warning)|message-input|attachment-(bar|pill|remove)|command-tag|ghost-hint|file-input)` → 0 hits.
- [ ] 9.5 Atom: chat-context-bar (banner, agent chip, pressure track, warning, compaction notice, run-status badge). Port + delete + visual validate.
- [ ] 9.6 Atom: session-sidebar (session-row grid, agent-tabs, delete-confirm dialog, search input, sidebar-foot). Port + delete + visual validate.
- [ ] 9.7 Atom: chat-canvas (canvas panel, header buttons, viewport, frame, spinner, overlay, debug panel, artifact panel head/body, fullscreen modifier). Port + delete + visual validate.
- [ ] 9.8 Atom: chat-popover (slash command palette, mention popover, prompt-template menu). Port + delete + visual validate.
- [ ] 9.9 Atom: approval-dialog (header, body, countdown, allow/deny actions). Port + delete + visual validate.
- [ ] 9.10 Atom: chat-widgets bucket (block-filter-bar, tool-ladder, transcript-search, message-actions, compaction-notice, empty-state, subagent-tree, file-block, image-block, json-tree, table-view, diff-view, code-view, markdown). Port + delete + visual validate. (May split into 9.10.x sub-tasks during execution.)
- [ ] 9.11 Final visual regression — Playwright screenshots of chat-rich + chat-empty + density toggle + light theme, all aligned to bundle screenshots. Update `?deckVisualState` baseline screenshot set in `docs/design-bundles/2026-04-29-claude-design-chat-pilot/project/screenshots/` mirror dir if useful.
- [ ] 9.12 Final theme.css audit — `grep "deck-ui-(" pattern selectors of all 190 chat-only classes from inventory; expected zero hits. Any remaining hits document why (e.g. shared with markdown shipped through `Markdown`atom that uses`.deck-ui-markdown`+`.ds-markdown` dual class as a shipping migration path).
- [ ] 9.13 P3 task 1 batch A had auto-unlock added (`923862790a`); confirm dev/E2E env still works after token rotation, restart real-stack at start of every 9.x atom session.
