## 1. Truth Mapping And Product Boundary

- [x] 1.1 Audit OpenClaw source truth for Skills: `AgentConfig.skills`, `skills.*` Gateway methods (`search / detail / bins / status / install / update`), `deck.agents.skills.get/set`, `plugin.approval.*`, the absence of `skills.uninstall` and `skills.rotate-apikey`, and the existing Deck-facing DTOs in `deck-go/contracts/source/deck-api.contract.ts` (`DeckGoSkillEntry`, `DeckGoSkillHub*`, `DeckGoPluginInventoryEntry`, `DeckGoPluginApprovalEntry`); record source file evidence before code changes.
- [x] 1.2 Build a Skills mutation and ownership ledger: each list, detail, action, navigation, and handoff affordance MUST map to a config domain or read source, an owning module, a Gateway/BFF route, a UI pattern, a risk level, and a verification check.
- [x] 1.3 Reconcile the implementation target with the design blueprint's information architecture: list workbench, detail hero, section nav, identity, Source & activation, API key, env, Eligibility health, Agent Usage (read-only), Owning Plugin, Danger zone.
- [x] 1.4 Classify cross-module surfaces as Skills-owned edit, Skills-owned navigation, owning-module navigation, or deferred handoff; remove or downgrade any UI concept that cannot be backed by Gateway/BFF truth.
- [x] 1.5 Compare the current frontend implementation, mock fixtures, latest Skills high-fidelity prototype, and existing implementation notes against the ownership ledger; document prototype-only fields (Files, Audit, trigger chips, marketplace metadata) as out-of-scope for this change.
- [x] 1.6 Lock visual authority in the implementation report: this OpenSpec product blueprint plus `frontend-new` design system outranks prototype pixel parity; the prototype is density/rhythm/reference only.
- [x] 1.7 Record a breaking-removal preflight: the Skills module's `SkillMatrixTab.onToggle` -> `updateAgentSkills` write path will be deleted; the `updateAgentSkills` wrapper itself is preserved for Agents.
- [x] 1.8 Produce a short implementation preflight note in the change evidence or implementation report that lists every intentional divergence from the current production UI and handoff prototype.

## 2. Contracts And Backend Control Surface

- [x] 2.1 Update Deck-facing Skills DTOs only for deterministic product semantics: `apiKeyConfigured`, `sourceRaw`, product `source`, `agentUsage`, `availableActions`, `unsupportedReasons`, and `owningPlugin` best-effort. Do NOT add `apiKeyHintLast4`, `installedVersion`, `availableVersion`, or per-skill `updateState` in this pass because current Gateway read contracts do not expose safe secret summaries or ClawHub tracking/version status. Do NOT add a separate `DeckGoSkillDetailResponse` and do NOT introduce a `GET /skills/{skillKey}` BFF route — the list response already carries every field the detail hero needs.
- [x] 2.2 Add `unsupportedReasons.uninstall = 'gateway-rpc-missing'` and `unsupportedReasons.rotate = 'gateway-rpc-missing'` derivation rules in the BFF normalization; do not expose any unsupported affordance.
- [x] 2.3 Implement BFF `agentUsage` join from `agents.list[].skills` so that the join happens server-side; preserve raw fields for power-user fallback.
- [x] 2.4 Implement BFF `owningPlugin` best-effort join via `deck.plugins.list` capabilities/toolNames/providerIds; explicitly return `null` when the join cannot resolve.
- [x] 2.5 Implement product source normalization from Gateway raw source strings: `openclaw-bundled -> bundled`, `openclaw-managed -> managed`, `openclaw-workspace -> workspace`, `openclaw-extra -> extra`, `agents-skills-personal -> personal`, `agents-skills-project -> project`, otherwise `unknown`; preserve the raw string in `sourceRaw`. Do not collapse all unknown/local sources to `bundled`.
- [x] 2.6 Do NOT implement per-skill `installedVersion`, `availableVersion`, or `updateState` derivation by joining `skills.status` with `skills.search`; that would be a false contract because `skills.status` has no ClawHub origin slug or installedVersion. Instead, set `unsupportedReasons.perSkillUpgrade = 'gateway-tracking-status-missing'` for loaded managed skills where operators would expect a per-skill update action, and expose only the guarded global update-all action when supported.
- [x] 2.7 Implement BFF `apiKeyConfigured` derivation from safe redacted config/status evidence; never include the saved value in the response payload and do NOT derive or expose last-4 hints in this pass. Add a `config` sanitizer applied on BOTH branches of `normalizeSkillsResponse` (typed `SkillsStatusResult` and the `deckSkillEntryFromMap` map fallback) that strips `config.apiKey` and any field whose key matches a known-secret list (e.g. names ending in `_TOKEN`, `_SECRET`, `_KEY`) before assigning to the response `Config`. Today the map fallback assigns `Config: coerce.Map(record["config"])` unchanged, so it MUST be replaced with the sanitized variant. Assert unit tests against both the derivation function and the sanitizer (positive: apiKey is configured but absent from response; negative: response without configured apiKey is unchanged).
- [x] 2.8 Add backend or adapter tests proving Skills product DTO fields are stable, source taxonomy is preserved, agent-matrix write attempts that originate from Skills routes are not introduced, the absence of uninstall/rotate/per-skill-upgrade/apiKey-hint contracts is reflected in `availableActions`/`unsupportedReasons`, the typed BFF rejects `skills.uninstall` with `INVALID_GATEWAY_METHOD` (because it is not in `internal/gateway/generated/allowlist.go`), and apiKey values never leak — covering BOTH the typed response branch and the map fallback branch of `normalizeSkillsResponse` by populating `entries[skillKey].apiKey` in the underlying config and asserting the response payload excludes it.
- [x] 2.9 Regenerate and verify generated contract artifacts after source DTO changes; do not hand-edit generated files.

## 3. Skills Frontend Product Implementation

- [x] 3.1 Rewrite `SkillsPanel.tsx` around product sections derived from the ownership ledger: toolbar, list workbench, detail hero with status badges, section nav, and a defined section set (Identity / Source & activation / API key / env / Eligibility health / Agent Usage / Owning Plugin / Danger zone).
- [x] 3.2 Delete `SkillHubTab.tsx`, `SkillMatrixTab.tsx`, and `SkillInfoTab.tsx`; migrate `SkillHubTab` search and detail affordances into wizard Step 2.
- [x] 3.3 Implement `InstallSkillWizard.tsx` (Source -> Slug & version preview -> apiKey optional -> env optional -> Review and confirm) with masking in review and predicted impact; reuse `searchSkillHub`, `fetchSkillHubDetail`, `installSkillHub`, and `updateSkill` wrappers. Step 2 shows the slug's latest published version as preview-only (no manual version picker) because current read APIs do not expose installed version afterward. Step 5 explicitly discloses the two-step write plan; on Confirm, call `installSkillHub(slug)` first, then resolve `skillKey` from the install handler result (`result.slug`) or by re-fetching `fetchSkills` and matching on slug, then chain `updateSkill(skillKey, {apiKey?, env?})` ONLY when at least one of the two fields was captured. Test the chain failure path: install ok + updateSkill failure leaves the skill installed and routes to a setup-checklist with a recoverable retry, NOT a wizard rollback.
- [x] 3.4 Rename `InstallSkillDialog.tsx` to `AddBinDialog.tsx`; preserve its current behavior; ensure tests are renamed and asserting only the per-bin behavior.
- [x] 3.5 Implement `SkillSecretDialog.tsx` with masked input, `apiKeyConfigured` display, Update / Clear actions, a disabled Rotate button labeled with `gateway-rpc-missing`, and copy explaining that last-4 display is unsupported until a safe Gateway secret-summary contract exists; never display the saved value.
- [x] 3.6 Implement `SkillEnvKeyValueEditor.tsx` with regex-validated keys (`^[A-Z][A-Z0-9_]*$`), empty-row handling, and a "Show raw JSON" disclosure that round-trips with the table view.
- [x] 3.7 Implement `SkillAgentUsageSection.tsx` as a read-only matrix slice listing agents that include this skill, with mode label `all / whitelist` and per-row link to Agents detail via `navigateToAgent`; verify there is no toggle button or `Save` affordance in this section.
- [x] 3.8 Implement `SkillUninstallHandoffBanner.tsx` that renders explicit `gateway-rpc-missing` copy in the detail Danger zone; verify there is no destructive button in any Skills component.
- [x] 3.9 Implement L0 inline toggle for `enable / disable` with a live "X agents in use" hint based on `agentUsage.count`; show a guard line when the count is greater than zero.
- [x] 3.10 Implement L1 guarded drawers for `update env` and global `update all ClawHub-managed skills to latest` with diff/broad-scope copy and affected-agent count. The global update-all drawer is enabled only when at least one loaded skill has product source `managed`; it saves through `updateSkillHub()` with no slug (BFF sends `skills.update {source:'clawhub', all:true}`). Do NOT introduce per-skill update availability, per-skill `updateSkillHub(slug)`, a version picker, or a `version` argument anywhere in the update flow. Env saves go through `updateSkill(skillKey, {env})`.
- [x] 3.11 Implement the L2 `update apiKey` and `clear apiKey` flow; ensure neither value nor the cleared transition leaks plaintext in the rendered DOM tree.
- [x] 3.12 Remove every direct `updateAgentSkills` call from Skills components; assert at compile time and at test time that Skills components do not import or invoke `updateAgentSkills`.
- [x] 3.13 Add the `Approvals: N pending` toolbar badge sourced from `plugin.approval.list`; the badge MUST link to the Approvals panel; clicking it MUST NOT open an inline Approvals editor inside Skills.
- [x] 3.14 Add the detail Owning Plugin section that shows plugin name and link to Plugins; render "unknown plugin" copy and disable the link when the join returns `null`.
- [x] 3.15 Update i18n strings for English and Chinese; verify no overflow in light/dark themes; preserve `frontend-new` design-system tokens, atoms, and patterns.

## 4. Focused Frontend And Mock Verification

- [x] 4.1 Rewrite `SkillsPanel.test.tsx`: drop matrix toggle assertions; add wizard step coverage, secret masking assertions, env validation assertions, guarded global ClawHub update-all drawer assertions, uninstall handoff banner assertions, agent-usage read-only assertions, and a regression assertion that Skills components do not invoke `updateAgentSkills`.
- [x] 4.2 Extend mock Skills fixtures to include: ready list with mixed statuses, source taxonomy variants (`bundled / managed / workspace / extra / personal / project / unknown`), agent-usage variants, multiple `owningPlugin` resolution states (resolved / null), needs-setup, disabled, ClawHub-degraded mode, unsupported per-skill-upgrade/apiKey-hint states, an install-success-then-updateSkill-failure variant for the chained-write recovery path, and pending approvals count.
- [x] 4.3 Run mock visual E2E for Skills covering ready list, selected detail (loaded and degraded), install wizard each step including review masking, secret dialog, env key-value editor with invalid key error, global update-all drawer, uninstall handoff banner, agent-usage read-only, owning plugin unknown state, unsupported per-skill-upgrade/apiKey-hint copy, empty/error/degraded, light/dark, and Chinese/English; capture evidence and label it as mock-only visual proof.
- [x] 4.4 Use Playwright against the hot-reload frontend to verify navigation into Skills, section interaction, dialogs and drawers, guarded saves, theme/language switching, and absence of obvious layout overflow.

## 5. Real Gateway Verification

- [x] 5.1 Prepare or reuse the isolated real E2E environment by copying global OpenClaw config and workspace into test-scoped paths; never mutate the user's global `openclaw.json` or workspace during verification.
- [x] 5.2 Use a preconfigured ClawHub staging fixture slug (e.g. `clawhub-staging-fixture-<env>`) as the install/upgrade target if one is documented for the run. ClawHub does not expose a publish RPC, so this change does NOT attempt to seed a fresh ClawHub slug from inside the test harness. When no staging slug is available, default the entire L1 mutation set to L2 `skipped-safe` and record the L-tier outcome explicitly. Local artifacts (apiKey value, env keys) MUST include the run id (e.g. `__deck_test_apikey_<runId>`); cleanup MUST refuse targets that do not match either the documented staging slug or the run-id-prefixed local artifact pattern.
- [x] 5.3 Run real Gateway read coverage for loaded Skills list/detail from list entries (`skills.status`), ClawHub catalog search and catalog detail (`skills.search`, `skills.detail` / `fetchSkillHubDetail`), ClawHub bins (`skills.bins`), agent-skill matrix read (`deck.agents.skills.get`), plugin inventory join (`deck.plugins.list`), and Approvals pending count (`plugin.approval.list`).
- [x] 5.4 Run real Gateway L1 mutation coverage against the staging fixture slug: install (slug only — Gateway clawhub install does NOT accept apiKey/env), then chained post-install `updateSkill(skillKey, {apiKey, env})` with a `__deck_test_apikey_<runId>` dummy and run-id-tagged env keys, then enable, disable, update env (separate edit), update apiKey, clear apiKey, and global update-all-to-latest via `updateSkillHub()` with NO slug and NO `version` param. If any mutation cannot run safely, skip that mutation only and record the L2 `skipped-safe` reason. Verify also that the install-success-then-updateSkill-failure path leaves the skill installed but `apiKeyConfigured: false` (chain is recoverable, not transactional).
- [x] 5.5 Run real negative coverage at the Deck typed BFF transport: a direct `skills.uninstall` request to `POST /runtimes/{runtimeId}/gateway/rpc` MUST return `INVALID_GATEWAY_METHOD` because the method is not present in `internal/gateway/generated/allowlist.go`. Recording method-not-found at the raw Gateway socket layer is permitted as an additional probe, but is NOT the canonical assertion because real users go through the typed BFF. UI components MUST NOT issue this RPC under any flow.
- [x] 5.6 Run real Gateway negative coverage: direct `updateAgentSkills` invocation MUST NOT originate from Skills components in any browser-driven test scenario; verify both at the network observation layer and at the React component import graph.
- [x] 5.7 If real verification is blocked by environment instability after two evidence-producing attempts, record the blocker, logs, attempted commands, and remaining risk in this change before proceeding to L2 or L3 fallback; code-level and mock verification MUST still pass.

## 6. Closure Gates

- [x] 6.1 Run `openspec validate deck-go-skills-product-control-plane --strict` and fix every spec/task formatting issue before implementation is considered complete.
- [x] 6.2 Run relevant contract gates, including `make protocol-check` and `make contract-gate`, ensuring generated artifacts are in sync and additive only.
- [x] 6.3 Run relevant Go backend tests for touched Skills BFF/runtime adapters.
- [x] 6.4 Run relevant frontend unit and component tests and `make frontend-build` for `frontend-new`.
- [x] 6.5 Run Skills mock visual verification and bounded real Gateway verification, or record the approved circuit-breaker handoff with evidence including the chosen real-verification tier (L1 / L2 / L3).
- [x] 6.6 Update `frontend-handoff/modules/skills/implementation-notes.md` with a product-control-plane closeout that records the breaking removal of the agent-matrix write path, the new IA, the four-level mutation safety, and the chosen verification tier.
- [x] 6.7 Update task checkboxes only after each task has implementation evidence, command output evidence, or an explicit handoff with reason.
- [x] 6.8 Produce an implementation report listing changed files, contract-chain decisions, product-design decisions, breaking-removal evidence, verification evidence, deferred handoffs, and archive readiness.
