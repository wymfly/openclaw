# Generated by dev-workflow for writing-plans consumption
mode: 1
pre_g2: likely

team:
  enabled: true
  collaboration_patterns:
    - cross_domain_parallel # Modules 2, 3, 4 are independent after Module 0+1
    - multi_module_coord # Shared event infrastructure across modules
  requirements:
    - file_list_per_task
    - cross_file_matrix
    - task_0_if_shared_interface # Module 0 = shared infrastructure
    - task_n_integration
    - resolve_conflicts
    - team_role_suggestion

execution:
  mode: subagent-driven
  reason: "5 modules with clear dependency chain; Module 0→1 serial, then 2/3/4 parallel"

# Design spec reference
design_spec: "docs/plans/2026-03-25-deck-upstream-sync-design.md"
