# Skill 摘要（由 orchestrator 从完整 Skill 和 CLAUDE.md 提取）

## 项目级质量约束（来自 CLAUDE.md，Codex 不可见，必须遵守）

- TypeScript checks: `pnpm tsgo` — 每次修改后运行，修复全部类型错误再 commit
- Lint/format: `pnpm check` — 提交前必须通过
- Tests: `pnpm test` (vitest) — 提交前运行完整测试套件
- 避免 `any` 类型；不添加 `@ts-nocheck`
- mock 对象必须包含所有必填字段（尤其时间戳字段）
- mock patch 路径 = 消费方的 import 路径（不是定义方的路径）
- 使用 `scripts/committer "<msg>" <file...>` 提交（不要手动 git add/commit）
- commit 消息格式：`[enhanced][codex-impl] <type>(deck): <description>`
- 不要添加不必要的 docstrings/comments/type annotations 到未修改的代码
- 保持文件简洁，under ~700 LOC

## Dashboard 特定约束（来自 dashboard/CLAUDE.md）

- 新增 Gateway RPC 方法必须同步 allowlist（本项目不新增 RPC，无需操作）
- `tsc --noEmit` 零错误后才能标记任务完成
- **i18n**：所有用户可见文字通过 `useTranslations()` 调用，禁止硬编码。`zh.json` 和 `en.json` 必须同步更新
- **主题**：使用 shadcn 标准变量 + 扩展令牌，禁止硬编码颜色（`#fff`/`bg-white`/`text-gray-*`）
- **HTML 合规**：`<button>` 内不可包含 `<button>`/`<a>`/交互元素
- 组件提交前检查清单：所有文字用 `t()`、颜色用 CSS 变量、无交互元素嵌套、新 RPC 加 allowlist

## TDD 关键约束（纪律型 — Phase 3 全程生效）

- NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST
- Red-Green-Refactor 三步不可跳步：先写失败测试 → 写最小实现 → 重构
- 每个测试验证一个行为
- 测试名称描述被测行为（"returns null when..."，"clears only targeted..."）
- 使用 `describe` 块按功能组织测试
- mock 只在真正需要隔离时使用（本项目用 in-memory SQLite，优先真实 DB 测试）
- 每个 task 完成后运行 `pnpm test -- <相关测试文件>` 验证
- 测试需清理 timers/env/globals/mocks

## 前端测试模式（本任务适用）

- Zustand store 测试：`vi.resetModules()` + 动态 `import("../chat")` 在 `beforeEach` 中
- Mock 模式：顶层 `vi.mock("path")`，然后在 `beforeEach` 中动态 import mocked module
- 使用 `vi.mocked(fn)` 做类型安全的 mock 断言
- 使用 `vi.useFakeTimers()` / `vi.useRealTimers()` 做时间相关测试
- Hook 测试：使用 `renderHook` from `@testing-library/react`，加 `@vitest-environment jsdom`
- `DEFAULT_EVICT_IDLE_MS = 5 * 60 * 1000`（from `@/stores/chat-types`）
- Dashboard 测试命令：`cd dashboard && pnpm test -- <test-file> -v`

## Gateway Protocol SDK 约束（deck-platform-hardening 特定）

- method-defs 必须放在独立文件（`*-method-defs.ts`），不能放在 handler 文件中（避免 codegen 导入运行时代码）
- 参考模式：`chat-method-defs.ts`、`sessions-method-defs.ts`
- Result schema 使用 TypeBox `Type.Object` 定义在 `src/gateway/protocol/schema/` 下
- `allMethodDefs` 在 `method-registry-data.ts` 中聚合：`import + spread` 新 defs
- `pnpm protocol:gen:ts` 重新生成 typed client
- `pnpm protocol:gen:check` 必须通过才能 commit
- `dashboard/src/types/gateway-*.generated.ts` 是自动生成的，NEVER 手工编辑
- `gwRequest()` = typed，`gatewayRequest()` = deprecated untyped，迁移方向是后者→前者
- typed 判定：`def.params && def.result` 同时存在（非仅有 params 或仅有 result）

## Panel Registry 约束（deck-platform-hardening 特定）

- ChatPanel 是 **eager static import**（不是 lazy）— 避免 flash-of-loading
- 其他所有面板使用 `lazy(() => import(...).then(m => ({ default: m.Export })))`
- `as const satisfies readonly PanelEntry[]` 保留字面量类型供 `Panel` type 派生
- `Panel` type = `(typeof PANELS)[number]["id"]`（从 registry 派生，不手写 union）
- Settings 面板使用 `position: "bottom"` 标记，NavRail 底部渲染
- 快捷键面板使用 `shortcutIndex` 字段（Alt+1~9）
- PanelComponent 类型：`ComponentType | LazyExoticComponent<ComponentType>`
- `pnpm build` 是 **hard gate**（lazy-loading 边界变更必须验证 build）
