# Deck Go Stream Contract

Generated by `make stream-contract-sync`.

This document explicitly contracts Deck Go SSE stream envelopes while stream payload DTO migration is staged.

## SSE Streams

### GET /stream

- Transport: `sse`
- Last-Event-ID: supported
- Heartbeat: comment: heartbeat every 15s
- Notes: Main Deck Go server event stream. Event frames use SSE id/event/data fields; data is JSON when an event payload contract exists.

| Event                    | Payload                                                                                        | Status                   | Source                                                                       | Notes                                                                                                                                                                                                                                                                                         |
| ------------------------ | ---------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `projection.gap`         | `DeckGoProjectionGapEvent`                                                                     | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Emitted when Last-Event-ID is older than the retained event buffer.                                                                                                                                                                                                                           |
| `chat`                   | `{ sessionKey?: string; runId?: string; state?: string; [key: string]: unknown }`              | `documented-dynamic`     | `deck-go/backend/internal/runtime/projection/activity.go`                    | Legacy chat event bus frame consumed by the specialized chat dispatcher and activity projection until all Gateway session events are canonicalized.                                                                                                                                           |
| `agent`                  | `{ sessionKey?: string; runId?: string; stream?: string; data?: Record<string, unknown> }`     | `documented-dynamic`     | `deck-go/backend/internal/runtime/projection/activity.go`                    | Legacy agent tool/lifecycle frame consumed by chat and activity projections.                                                                                                                                                                                                                  |
| `canvas`                 | `{ action?: string; params?: Record<string, unknown>; [key: string]: unknown }`                | `documented-dynamic`     | `deck-go/frontend-new/src/components/panels/chat/useChatSSE.ts`              | Canvas/A2UI bridge frame consumed by the specialized chat dispatcher.                                                                                                                                                                                                                         |
| `runtime.gateway.status` | `DeckGoRuntimeGatewayStatus`                                                                   | `temporary-frontend-dto` | `deck-go/frontend/src/api.ts`                                                | Runtime status DTO is still frontend-local until the runtime/settings migration batch moves it into deck-api.contract.ts.                                                                                                                                                                     |
| `runtime.gateway.health` | `DeckGoRuntimeGatewayStatus`                                                                   | `temporary-frontend-dto` | `deck-go/frontend/src/api.ts`                                                | Runtime health payload shares the runtime gateway status shape during staged migration.                                                                                                                                                                                                       |
| `runtime.gateway.exit`   | `DeckGoRuntimeGatewayStatus`                                                                   | `temporary-frontend-dto` | `deck-go/frontend/src/api.ts`                                                | Local runtime exit payload shares the runtime gateway status shape during staged migration.                                                                                                                                                                                                   |
| `session.message`        | `DeckGoSessionMessageStreamEvent`                                                              | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Session message projection update consumed by chat panels.                                                                                                                                                                                                                                    |
| `session-msg`            | `DeckGoSessionMessageStreamEvent`                                                              | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Legacy alias accepted by the chat dispatcher while Gateway session event naming converges.                                                                                                                                                                                                    |
| `session.tool`           | `DeckGoSessionToolStreamEvent`                                                                 | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Tool activity projection update consumed by chat panels.                                                                                                                                                                                                                                      |
| `session-tool`           | `DeckGoSessionToolStreamEvent`                                                                 | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Legacy alias accepted by the chat dispatcher while Gateway session event naming converges.                                                                                                                                                                                                    |
| `sessions.changed`       | `DeckGoSessionsChangedStreamEvent`                                                             | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Session lifecycle update consumed by chat/session projections.                                                                                                                                                                                                                                |
| `session-state`          | `DeckGoSessionsChangedStreamEvent`                                                             | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Legacy alias accepted by the chat dispatcher while Gateway session event naming converges.                                                                                                                                                                                                    |
| `approval.pending`       | `DeckGoPendingApproval`                                                                        | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Pending approval projection consumed by chat and approvals panels.                                                                                                                                                                                                                            |
| `approval.resolved`      | `{ id?: string; sessionKey?: string }`                                                         | `documented-dynamic`     | `deck-go/frontend-new/src/components/panels/approvals/useApprovalsStream.ts` | Approval resolution projection; the current BFF only needs id/sessionKey for removal and chat approval cleanup.                                                                                                                                                                               |
| `device.pair.requested`  | `{ deviceId?: string; name?: string; label?: string; actor?: string; [key: string]: unknown }` | `documented-dynamic`     | `deck-go/frontend-new/src/components/panels/settings/SettingsPanel.tsx`      | Device pairing advisory update; GET /devices remains authoritative.                                                                                                                                                                                                                           |
| `device.pair.resolved`   | `{ deviceId?: string; name?: string; label?: string; actor?: string; [key: string]: unknown }` | `documented-dynamic`     | `deck-go/frontend-new/src/components/panels/settings/SettingsPanel.tsx`      | Device pairing resolution advisory update; GET /devices remains authoritative.                                                                                                                                                                                                                |
| `commands.changed`       | `{ version?: string; agentId?: string; [key: string]: unknown }`                               | `documented-dynamic`     | `deck-go/frontend-new/src/hooks/use-command-discovery.ts`                    | Command registry invalidation event; POST /deck/commands/discover remains authoritative.                                                                                                                                                                                                      |
| `activity.event`         | `DeckGoActivityStreamEvent`                                                                    | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Agents panel activity metrics stream; payload carries `agentId` + `type` + open envelope. Reverse-engineered from useAgentMetricsSSE.ts; deck-go middleware is a transparent SSE pipe (stream.go), so any upstream drift is caught by the backend narrowing test stream_event_typing_test.go. |
| `agent.status.changed`   | `DeckGoAgentStatusChangedStreamEvent`                                                          | `generated-dto`          | `deck-go/contracts/source/deck-api.contract.ts`                              | Agent busy/idle status transition. `status` is `"busy"                                                                                                                                                                                                                                        | "idle"`today with`(string & {})` extension to tolerate new upstream values without a contract change. Backend narrowing test asserts this in stream_event_typing_test.go. |

### GET /logs/stream

- Transport: `sse`
- Last-Event-ID: supported
- Heartbeat: comment: heartbeat every 15s
- Notes: Log tail stream. The outer event envelope is generated; log payload leaves stay dynamic until logs.tail receives a stable schema.

| Event       | Payload                                  | Status               | Source                                      | Notes                                                                                          |
| ----------- | ---------------------------------------- | -------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `log.batch` | `{ lines?: unknown[]; cursor?: number }` | `documented-dynamic` | `deck-go/backend/internal/server/stream.go` | Payload mirrors logs.tail dynamic lines and cursor fields; covered by the logs.tail exception. |
| `log.reset` | `Record<string, never>`                  | `documented-dynamic` | `deck-go/backend/internal/server/stream.go` | Emitted when the log cursor resets.                                                            |

## JSON Stream Control Endpoints

| Endpoint                    | Request                      | Response                      | Source                                          | Notes                                                                                    |
| --------------------------- | ---------------------------- | ----------------------------- | ----------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `POST /chat/session-events` | `DeckGoSessionEventsRequest` | `DeckGoSessionEventsResponse` | `deck-go/contracts/source/deck-api.contract.ts` | JSON control endpoint used to subscribe or unsubscribe Gateway session event projection. |
