# Deck Go Live Projection Contract

Generated by `make live-projection-contract-sync`.

This document maps Deck Go product projections to their BFF stream source, refresh endpoint, stale-state threshold, and projection-gap policy. Code remains the final truth; regenerate this document from `contracts/source/deck-live-projections.contract.json` after source changes.

## Streams

| ID | Endpoint | Transport | Last-Event-ID | Gap Event | Notes |
| --- | --- | --- | --- | --- | --- |
| `deck-main` | `GET /stream` | `sse` | yes | `projection.gap` | Shared Deck Go control stream backed by the runtime event bus. |
| `deck-logs` | `GET /logs/stream` | `sse` | yes | none | Log tail stream backed by logs.tail polling. |

## Projections

| ID | Panel | Stream | Events | Refresh Endpoints | Stale After | Gap Policy | Cursor Key | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `agent-status` | `agents` | `deck-main` | agent.status.changed, activity.event, projection.gap | GET /deck/agents?agentId=... | 30000ms | `refresh` | `deckGoLiveProjection:agent-status:lastEventId` | Agent list/detail projections recover from the agent inventory BFF. |
| `activity-feed` | `activity` | `deck-main` | activity.event, chat, agent, projection.gap | GET /activity, GET /monitor/runs, GET /monitor/stats | 30000ms | `refresh` | `deckGoLiveProjection:activity-feed:lastEventId` | Activity and monitor projections recover by reloading read models from Deck Go BFF endpoints. |
| `approval-queue` | `approvals` | `deck-main` | approval.pending, approval.resolved, projection.gap | GET /approvals/pending, GET /approvals | 30000ms | `refresh` | `deckGoLiveProjection:approval-queue:lastEventId` | Pending approval state is opportunistic live state and recovers from the pending approvals endpoint. |
| `device-pairing` | `settings` | `deck-main` | device.pair.requested, device.pair.resolved, projection.gap | GET /devices, GET /devices/self | 30000ms | `refresh` | `deckGoLiveProjection:device-pairing:lastEventId` | Device pairing live updates are advisory; the device list endpoint remains authoritative. |
| `command-discovery` | `shell` | `deck-main` | commands.changed, projection.gap | POST /deck/commands/discover | 30000ms | `refresh` | `deckGoLiveProjection:command-discovery:lastEventId` | Command registry refreshes when Gateway-discovered commands change. |
| `chat-session` | `chat` | `deck-main` | chat, agent, session.message, session.tool, sessions.changed, session-msg, session-tool, session-state, approval.pending, approval.resolved, canvas, projection.gap | GET /chat/snapshot, GET /sessions, POST /chat/session-events | 30000ms | `refresh` | `deckGoLiveProjection:chat-session:lastEventId` | Chat keeps a specialized dispatcher but follows the same gap-to-refresh policy. |
| `log-tail` | `logs` | `deck-logs` | log.batch, log.reset | GET /logs | 30000ms | `none` | `deckGoLogsLastEventId` | Log tailing persists Last-Event-ID independently from the log cursor used by GET /logs. |
| `session-list` | `sessions` | `deck-main` | sessions.changed, session-state, projection.gap | GET /sessions | 30000ms | `refresh` | `deckGoLiveProjection:session-list:lastEventId` | Session list projections recover by reloading the session list BFF. |
| `routing-bindings` | `routing` | refresh-only | none | GET /deck/routing | 60000ms | `none` | none | Routing is refresh-only until Gateway exposes a routing change event. |
| `subagent-topology` | `subagents` | refresh-only | none | GET /deck/subagents | 60000ms | `none` | none | Subagent topology is refresh-only until Gateway exposes a topology change event. |
| `usage-observability` | `usage` | refresh-only | none | GET /usage/cost, GET /usage/providers, GET /usage/sessions | 60000ms | `none` | none | Usage is read-model refresh only; live deltas are not claimed. |
