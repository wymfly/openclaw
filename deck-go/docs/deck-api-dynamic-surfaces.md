# Deck API Dynamic Surfaces

Generated from `deck-go/contracts/source/deck-api-dynamic-surfaces.contract.json` and `deck-go/contracts/source/deck-api.contract.ts`.

This report documents intentional dynamic leaves in the Deck-facing API. Code truth remains the source contract and generated artifacts.

Dynamic leaves: 64

| Surface                                                                | Kind                      | Line | Classification                        | Owner        | Reason                                                                                                    | Exit criteria                                                                                             |
| ---------------------------------------------------------------------- | ------------------------- | ---: | ------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `DeckGoActivityStreamEvent.<index-signature>`                          | `index-signature-unknown` | 2240 | `sse-extension-envelope`              | activity     | Activity stream events carry upstream event fields beyond the stable agentId/type pair.                   | Replace with event-specific DTOs when activity stream payload schemas are productized.                    |
| `DeckGoAgentDetailResponse.<type>.sandbox`                             | `unknown`                 | 1166 | `config-fragment`                     | agents       | Agent sandbox settings mirror user config and may include mode-specific keys.                             | Narrow when OpenClaw publishes a stable sandbox config schema for agents.                                 |
| `DeckGoAgentHealthSnapshot.<type>.<index-signature>`                   | `index-signature-unknown` | 1178 | `gateway-status-envelope`             | agents       | Health snapshots preserve Gateway diagnostic fields not yet modeled as product KPIs.                      | Retire after the agents health panel consumes named Gateway health DTO fields only.                       |
| `DeckGoAgentHealthSnapshot.<type>.agents`                              | `array-record-unknown`    | 1177 | `gateway-status-envelope`             | agents       | Per-agent health details are passed through from Gateway health/status data.                              | Replace with named agent health DTOs when the product view fixes the required metrics.                    |
| `DeckGoAgentRawConfig.<type>.defaults`                                 | `record-unknown`          | 1183 | `config-fragment`                     | agents       | Raw agent config exposes editable OpenClaw config fragments.                                              | Remove from the product surface or type field-by-field when raw config editing is redesigned.             |
| `DeckGoAgentRawConfig.<type>.entry`                                    | `record-unknown`          | 1184 | `config-fragment`                     | agents       | Raw agent config entry preserves arbitrary OpenClaw agent config keys.                                    | Remove from the product surface or type field-by-field when raw config editing is redesigned.             |
| `DeckGoAgentRawConfig.<type>.list`                                     | `array-record-unknown`    | 1185 | `config-fragment`                     | agents       | Raw agent config list preserves arbitrary OpenClaw agent config entries.                                  | Remove from the product surface or type field-by-field when raw config editing is redesigned.             |
| `DeckGoAgentSubagentConfigResponse.<type>.effectiveThinking`           | `unknown`                 | 1240 | `config-fragment`                     | subagents    | Effective thinking config is derived from upstream agent config and may vary by model/provider.           | Narrow when OpenClaw exposes a stable thinking config schema.                                             |
| `DeckGoCanvasBridgeEvalRequest.result`                                 | `unknown`                 | 2397 | `action-result-envelope`              | chat         | Canvas bridge evaluation results are emitted by arbitrary canvas hosts.                                   | Introduce discriminated result DTOs if canvas host result kinds become fixed product features.            |
| `DeckGoCatalogProvider.<type>.<index-signature>`                       | `index-signature-unknown` | 1073 | `extension-envelope`                  | models       | Provider catalog entries keep upstream and provider-specific metadata.                                    | Remove after provider catalog metadata is fully modeled in Deck product DTOs.                             |
| `DeckGoChannelsStatusResponse.channelAccounts`                         | `record-unknown`          |  566 | `gateway-status-envelope`             | channels     | Channel account state is currently a Gateway status map keyed by channel.                                 | Replace with named channel account DTOs when channel account management is productized.                   |
| `DeckGoChannelsStatusResponse.channels`                                | `record-unknown`          |  565 | `gateway-status-envelope`             | channels     | Channel status is currently a Gateway status map keyed by channel.                                        | Replace with named channel status DTOs when all channel state fields are fixed.                           |
| `DeckGoChatProjectionRequest.a2uiState`                                | `record-unknown`          | 2375 | `chat-runtime-envelope`               | chat         | A2UI state is a frontend/runtime projection payload passed through until the projection handler is fixed. | Narrow in the chat projection handler follow-up after persistence semantics are implemented.              |
| `DeckGoChatSessionPatchRequest.<index-signature>`                      | `index-signature-unknown` | 2362 | `gateway-forward-compatible-envelope` | chat         | Session patch forwards known fields plus upstream-compatible extra session metadata keys.                 | Remove when sessions.patch accepts only the named Deck product fields.                                    |
| `DeckGoChatSnapshotResponse.a2uiState`                                 | `record-unknown`          |  542 | `chat-runtime-envelope`               | chat         | A2UI state comes from upstream timeline detail and is not yet a Deck-owned schema.                        | Narrow after upstream timeline/A2UI state schema is documented and adopted.                               |
| `DeckGoCompactionActionResponse.<type>`                                | `record-unknown`          |  762 | `action-result-envelope`              | chat         | Compaction action responses vary by action and currently share a broad result envelope.                   | Replace with action-discriminated response DTOs when compaction UX requires per-action fields.            |
| `DeckGoConfigLookupChild.<type>.hint`                                  | `record-unknown`          | 1114 | `gateway-schema-envelope`             | config       | Config lookup hints are schema fragments derived from OpenClaw config metadata.                           | Narrow only if OpenClaw publishes a stable hint schema.                                                   |
| `DeckGoConfigLookupResponse.<type>.hint`                               | `record-unknown`          | 1121 | `gateway-schema-envelope`             | config       | Config lookup hints are schema fragments derived from OpenClaw config metadata.                           | Narrow only if OpenClaw publishes a stable hint schema.                                                   |
| `DeckGoConfigLookupResponse.<type>.schema`                             | `record-unknown`          | 1120 | `gateway-schema-envelope`             | config       | Config lookup schema is a JSON schema fragment rather than a fixed product DTO.                           | Keep dynamic unless the config editor moves to a fixed form schema.                                       |
| `DeckGoConfigSnapshotResponse.<type>.config`                           | `unknown`                 | 1015 | `config-fragment`                     | config       | Config snapshot exposes arbitrary openclaw.json content.                                                  | Replace with a typed product projection if raw config snapshot stops being exposed.                       |
| `DeckGoContextWeightReport.<type>.<index-signature>`                   | `index-signature-unknown` | 2086 | `extension-envelope`                  | usage        | Context weight reports allow extra measurement fields from runtime analysis.                              | Remove when context weight reporting has a frozen metric set.                                             |
| `DeckGoCronJob.<type>.delivery`                                        | `unknown`                 | 1460 | `action-result-envelope`              | cron         | Cron delivery output depends on wake mode and downstream delivery path.                                   | Replace with delivery-kind DTOs when cron delivery modes are productized.                                 |
| `DeckGoCronJob.<type>.payload.<index-signature>`                       | `index-signature-unknown` | 1459 | `action-payload-envelope`             | cron         | Cron payload carries action-specific systemEvent or agentTurn fields.                                     | Replace with discriminated cron payload DTOs when all wake payload fields are fixed.                      |
| `DeckGoCronJobInput.<type>.payload.<index-signature>`                  | `index-signature-unknown` | 1476 | `action-payload-envelope`             | cron         | Cron input payload carries action-specific systemEvent or agentTurn fields.                               | Replace with discriminated cron payload DTOs when all wake payload fields are fixed.                      |
| `DeckGoCronRunEntry.<type>.delivery`                                   | `unknown`                 | 1489 | `action-result-envelope`              | cron         | Cron run delivery result depends on the executed delivery path.                                           | Replace with delivery-kind DTOs when cron delivery modes are productized.                                 |
| `DeckGoDeviceTokenRotateResponse.<type>`                               | `record-unknown`          | 1396 | `action-result-envelope`              | identity     | Device token rotation currently returns token plus compatibility metadata.                                | Narrow to token-only or a named token rotation DTO after identity APIs settle.                            |
| `DeckGoGatewayBatchCall.params`                                        | `unknown`                 |  226 | `gateway-rpc-envelope`                | api-explorer | Gateway batch params must carry arbitrary method params selected at runtime.                              | Keep dynamic; per-method typing belongs to generated Gateway clients, not the batch envelope.             |
| `DeckGoGatewayBatchError.details`                                      | `unknown`                 |  242 | `gateway-rpc-envelope`                | api-explorer | Gateway error details vary by method and failure source.                                                  | Narrow only if Gateway standardizes error detail schemas.                                                 |
| `DeckGoGatewayBatchResultEntry.result`                                 | `unknown`                 |  250 | `gateway-rpc-envelope`                | api-explorer | Gateway batch results return arbitrary method results selected at runtime.                                | Keep dynamic; per-method typing belongs to generated Gateway clients, not the batch envelope.             |
| `DeckGoGatewayDescribeEvent.payload`                                   | `record-unknown`          |  670 | `gateway-schema-envelope`             | api-explorer | Gateway describe event payloads are JSON schema objects.                                                  | Keep dynamic unless JSON schema itself is modeled as a first-class DTO.                                   |
| `DeckGoGatewayDescribeMethod.params`                                   | `record-unknown`          |  664 | `gateway-schema-envelope`             | api-explorer | Gateway describe method params are JSON schema objects.                                                   | Keep dynamic unless JSON schema itself is modeled as a first-class DTO.                                   |
| `DeckGoGatewayDescribeMethod.result`                                   | `record-unknown`          |  665 | `gateway-schema-envelope`             | api-explorer | Gateway describe method results are JSON schema objects.                                                  | Keep dynamic unless JSON schema itself is modeled as a first-class DTO.                                   |
| `DeckGoGatewayHealthResponse.agents`                                   | `record-unknown`          |  181 | `gateway-status-envelope`             | gateway      | Gateway health agent details remain a status envelope for mixed Gateway versions.                         | Replace after health response schema becomes stable enough for product DTOs.                              |
| `DeckGoGatewayHealthResponse.channels`                                 | `record-unknown`          |  183 | `gateway-status-envelope`             | gateway      | Gateway health channel details remain a status envelope for mixed Gateway versions.                       | Replace after health response schema becomes stable enough for product DTOs.                              |
| `DeckGoGatewayHealthResponse.sessions.recent`                          | `array-unknown`           |  182 | `gateway-status-envelope`             | gateway      | Recent session health rows are pass-through diagnostics.                                                  | Replace with named session health rows when the gateway panel fixes its metrics.                          |
| `DeckGoGatewayInvokeResult.body`                                       | `unknown`                 |  261 | `gateway-rpc-envelope`                | api-explorer | Gateway single-method RPC results return arbitrary method results selected at runtime.                    | Keep dynamic; per-method typing belongs to generated Gateway clients, not the single-method RPC envelope. |
| `DeckGoGatewayStatusResponse.channels`                                 | `record-unknown`          |  220 | `gateway-status-envelope`             | gateway      | Gateway status channels are keyed runtime diagnostics.                                                    | Replace after status channel diagnostics are productized.                                                 |
| `DeckGoGatewayStatusResponse.heartbeat.agents`                         | `record-unknown`          |  199 | `gateway-status-envelope`             | gateway      | Heartbeat agents may include extra runtime status fields across Gateway versions.                         | Replace after heartbeat agent rows have a stable Gateway schema.                                          |
| `DeckGoGatewayStatusResponse.sessions.byAgent`                         | `array-unknown`           |  214 | `gateway-status-envelope`             | gateway      | Session by-agent diagnostics are pass-through Gateway status rows.                                        | Replace with named rows when Gateway status exposes stable session aggregates.                            |
| `DeckGoGatewayStatusResponse.sessions.recent`                          | `array-unknown`           |  218 | `gateway-status-envelope`             | gateway      | Recent session diagnostics are pass-through Gateway status rows.                                          | Replace with named rows when Gateway status exposes stable session summaries.                             |
| `DeckGoLogStreamEvent.json`                                            | `unknown`                 |  728 | `sse-json-envelope`                   | logs         | Log stream JSON payload varies by upstream event kind.                                                    | Replace with event-specific log stream DTOs when log event kinds are fixed.                               |
| `DeckGoModelAuthOverviewResponse.<type>.payload.<index-signature>`     | `index-signature-unknown` | 1053 | `extension-envelope`                  | models       | Model auth payload may include provider-specific diagnostics.                                             | Remove after model auth overview fields are fully named.                                                  |
| `DeckGoModelAuthProvider.<type>.<index-signature>`                     | `index-signature-unknown` | 1046 | `extension-envelope`                  | models       | Provider auth rows may include provider-specific metadata.                                                | Remove after provider auth rows are fully productized.                                                    |
| `DeckGoModelCatalogProvidersResponse.<type>.payload.<index-signature>` | `index-signature-unknown` | 1080 | `extension-envelope`                  | models       | Model catalog payload may include provider-specific metadata.                                             | Remove after catalog response payloads are fully named.                                                   |
| `DeckGoModelProbeResponse.<type>.payload.<index-signature>`            | `index-signature-unknown` | 1099 | `extension-envelope`                  | models       | Model probe payload may include provider-specific diagnostics.                                            | Remove after probe response payloads are fully named.                                                     |
| `DeckGoNodeInvokeResponse.<type>.payload`                              | `unknown`                 | 1744 | `action-result-envelope`              | nodes        | Node command invoke payload depends on command and node implementation.                                   | Introduce command-specific result DTOs if node commands become fixed product actions.                     |
| `DeckGoNodePendingWorkItem.<type>.payload`                             | `record-unknown`          | 1760 | `action-payload-envelope`             | nodes        | Pending node work payload is optional and action-specific.                                                | Narrow when pending work types have stable payload schemas.                                               |
| `DeckGoRuntimeConfiguredModel.metadata`                                | `record-unknown`          |  695 | `extension-envelope`                  | models       | Configured model metadata carries provider-specific fields.                                               | Remove after all displayed model metadata fields are promoted to named DTO fields.                        |
| `DeckGoServerEvent.json`                                               | `unknown`                 |  717 | `sse-json-envelope`                   | runtime      | Generic server event JSON carries event-specific payloads.                                                | Replace with event-specific SSE DTOs for routes that have stable event kinds.                             |
| `DeckGoSessionCreateResponse.entry`                                    | `record-unknown`          |  326 | `chat-runtime-envelope`               | chat         | Session create entry is a Gateway session record pass-through.                                            | Replace with DeckGoSessionMeta when session create response no longer needs raw entry data.               |
| `DeckGoSessionCreateResponse.runError`                                 | `unknown`                 |  325 | `action-result-envelope`              | chat         | Run startup error can carry upstream error payloads.                                                      | Narrow after run startup errors are normalized by the BFF.                                                |
| `DeckGoSessionDetailResponse.a2uiState`                                | `record-unknown`          |  485 | `chat-runtime-envelope`               | chat         | A2UI state comes from upstream timeline detail and is not yet a Deck-owned schema.                        | Narrow after upstream timeline/A2UI state schema is documented and adopted.                               |
| `DeckGoSessionMutationResponse.entry`                                  | `record-unknown`          |  345 | `chat-runtime-envelope`               | chat         | Session mutation entry is a Gateway session record pass-through.                                          | Replace with DeckGoSessionMeta when mutation responses no longer expose raw entries.                      |
| `DeckGoSessionToolStreamEvent.data`                                    | `record-unknown`          |  503 | `sse-json-envelope`                   | chat         | Tool stream data carries tool-specific event payloads.                                                    | Introduce tool-event DTOs when tool stream event kinds are fixed.                                         |
| `DeckGoSettings.appearance`                                            | `record-unknown`          |   20 | `config-fragment`                     | settings     | Appearance settings preserve open UI config fragments.                                                    | Narrow when settings UI owns a fixed appearance schema.                                                   |
| `DeckGoSettings.notifications`                                         | `record-unknown`          |   21 | `config-fragment`                     | settings     | Notification settings preserve open UI config fragments.                                                  | Narrow when settings UI owns a fixed notification schema.                                                 |
| `DeckGoSettings.pairedDevices`                                         | `array-record-unknown`    |   22 | `config-fragment`                     | settings     | Paired device settings may include legacy or client-specific config fields.                               | Replace with DeckGoPairedDevice if settings stops exposing raw paired device config.                      |
| `DeckGoSkillEntry.<type>.config`                                       | `record-unknown`          |  782 | `config-fragment`                     | skills       | Skill config carries skill-specific options.                                                              | Narrow with per-skill config schemas if skills become fixed product forms.                                |
| `DeckGoSkillUpdateResponse.<type>.config`                              | `record-unknown`          |  797 | `config-fragment`                     | skills       | Skill update response returns the skill-specific config fragment.                                         | Narrow with per-skill config schemas if skills become fixed product forms.                                |
| `DeckGoSubagentRun.<type>.outcome`                                     | `unknown`                 |  966 | `action-result-envelope`              | subagents    | Subagent run outcome depends on child agent task execution.                                               | Replace with named outcome DTOs after subagent completion states are productized.                         |
| `DeckGoTranscriptToolUseBlock.input`                                   | `record-unknown`          |  426 | `action-payload-envelope`             | chat         | Tool input payload depends on the invoked tool schema.                                                    | Keep dynamic; tool-specific schema rendering belongs to tool metadata.                                    |
| `DeckGoTranscriptUnknownBlock.summary`                                 | `record-unknown`          |  455 | `chat-runtime-envelope`               | chat         | Unknown transcript blocks intentionally preserve a summarized unsupported raw block.                      | Remove when all upstream transcript block types have DeckGoTranscriptBlock variants.                      |
| `DeckGoUsageSessionsResponse.<type>.aggregates.<index-signature>`      | `index-signature-unknown` | 2171 | `extension-envelope`                  | usage        | Usage aggregates allow future aggregate dimensions while named dimensions are already typed.              | Remove when usage aggregate dimensions are frozen.                                                        |
| `DeckGoUsageTotals.<type>.<index-signature>`                           | `index-signature-unknown` | 2048 | `extension-envelope`                  | usage        | Usage totals allow additional token/cost counters beyond currently displayed fields.                      | Remove when the usage totals metric set is frozen.                                                        |
