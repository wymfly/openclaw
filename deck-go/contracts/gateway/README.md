# Gateway Export Bundle

This directory will contain the Gateway-derived export bundle used by `deck-go/`.

Planned workflow:

1. export from the current Gateway authority
2. generate TS artifacts for the new frontend
3. generate Go bindings for the new backend
4. validate generated outputs in CI
