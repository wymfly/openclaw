# Dev Helpers

This directory owns local development helpers for running legacy Deck and `deck-go/` side-by-side.

Phase 0 scope:

- establish stable paths and script names
- avoid assuming backend/frontend implementation is ready
- make future side-by-side comparisons easy to standardize
